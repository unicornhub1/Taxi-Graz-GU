# TinaCMS-Integration – Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TinaCMS mit Visual Editing in die bestehende Next-16-Seite taxigraz-gu.at integrieren, sodass der Kunde alle Inhalte, Rechtstexte und die Akzentfarbe/Bilder unter `/admin` pflegen kann – ohne dass sich das Design verändert.

**Architecture:** Inhalte wandern aus `src/data/*.ts`, `src/lib/constants.ts` und den Komponenten in JSON/MDX-Dateien unter `content/`. Server-Pages holen die Daten über den generierten Tina-Client und reichen `{data, query, variables}` an Client-Wrapper mit `useTina` weiter; die bestehenden Sektions-Komponenten bekommen Daten als Props und `data-tina-field`-Attribute, ihr Markup/Klassen bleiben unverändert. Globale Einstellungen laufen über einen React-Context (`useSettings`), die Akzentfarbe wird als CSS-Variablen auf `<html>` gesetzt.

**Tech Stack:** Next.js 16.1.6 (App Router), React 19.2.3, Tailwind 4, TypeScript strict, `tinacms@^3.12.1`, `@tinacms/cli@^2.6.1`, `vitest` (nur für Helper-Tests), Vercel.

**Spec:** `docs/superpowers/specs/2026-09-01-tina-cms-design.md`

## Global Constraints

- Node 22 / npm 10, macOS + zsh: Globs quoten, **kein `timeout`**, kein Foreground-`sleep` (Warten auf Server via `curl --retry`).
- Sprache: alle Tina-Labels, Kommentare, Commit-Messages auf **Deutsch**. Code-Bezeichner Englisch.
- **Design bleibt unverändert:** In Sektions-Komponenten dürfen nur (a) Datenquellen (Props statt Import), (b) `data-tina-field`-Attribute und (c) die drei explizit genannten Farbstellen (`#E8B931` im Logo, `rgba(232,185,49,0.3)`-Grid, OG-Image) geändert werden. Keine Klassen anfassen.
- Fonts bleiben fix (DM Serif Display / Outfit via `next/font`).
- **Abbruchregel:** Wenn `tinacms dev`, die Admin-Route oder `useTina` unter Next 16 / React 19 strukturell nicht funktionieren (nicht nur ein Tippfehler), sofort stoppen und an Felix berichten – nicht mit Workarounds weiterbasteln.
- Lint-Baseline (vor Umbau): **1 Error** (`src/components/ui/CookieConsent.tsx:72` react-hooks/set-state-in-effect, vorbestehend, nicht anfassen) + 3 Warnings (unused imports). Nach jedem Task: **keine neuen** Lint-Probleme.
- Keine Secrets in Commits (`.env*` bleibt ignoriert). Tina-Credentials kommen erst bei Felix' Cloud-Setup nach Vercel.
- Commit-Messages: deutsches Präfix (`feat(cms):`, `refactor:` …), Trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` und `Claude-Session: https://claude.ai/code/session_011JNsY4XGU42Xi7j3fhrd3f`.
- Arbeitsverzeichnis: `/Users/felixzink/UnicornFactory/Kundenprojekte/Taxigraz Gu/taxi-graz-gu` auf Branch `feat/tina-cms`. Kein Merge/Push ohne Felix.
- Scratchpad für Screenshots/Logs: `SCRATCH="/private/tmp/claude-501/-Users-felixzink-UnicornFactory-Kundenprojekte-Taxigraz-Gu-taxi-graz-gu/b717371b-4da3-4a7c-a72d-42cf85784765/scratchpad"`.
- Dev-Server starten/stoppen (überall gleich):
  ```bash
  cd "/Users/felixzink/UnicornFactory/Kundenprojekte/Taxigraz Gu/taxi-graz-gu" && (npm run dev > "$SCRATCH/dev.log" 2>&1 &) && curl --retry 60 --retry-delay 2 --retry-all-errors -s -o /dev/null http://localhost:3000/ && echo "dev läuft"
  # stoppen:
  lsof -ti tcp:3000,4001,9000 | xargs kill 2>/dev/null; echo gestoppt
  ```

## Abweichungen von der Spec (bewusst, klein)

1. **`footer.description` liegt in den Einstellungen** (`settings.footer.description`) statt in `home.json` – der Footer ist global (Layout), nicht Teil der Startseite.
2. **Keine kombinierten Custom-Queries** (`tina/queries/*.gql`): pro Seite reicht der Standard-Query der Collection (`client.queries.home` …); die Einstellungen kommen über den Context aus dem Layout-Wrapper `SiteShell`. Weniger Fragment-Magie, gleiche Funktion.
3. **TrustBar-Icons entfallen** – die bestehende Komponente rendert die Icons gar nicht, nur `value` + `label`.
4. **Firmenblock auf Rechtsseiten per Frontmatter-Flag** `showCompanyBlock` (Impressum `true`, Datenschutz `false`) statt fester Zuordnung im Code.
5. Reihenfolge im Impressum: der Firmenblock (§ 5 ECG, Kontakt, UID, Firmenbuch) steht gesammelt oben, „Unternehmensgegenstand" rutscht dahinter.

---

### Task 1: Baseline-Screenshots & Build-Check

**Files:**
- Create: `$SCRATCH/shots.sh` (nicht im Repo)

**Interfaces:**
- Produces: `$SCRATCH/before/{home,kontakt,impressum,datenschutz}-{1440,390}.png` – Referenz für Task 10.

- [ ] **Step 1: Screenshot-Skript anlegen**

```bash
cat > "$SCRATCH/shots.sh" <<'EOF'
#!/bin/zsh
# Nutzung: zsh shots.sh <ausgabeordner> [basis-url]
set -e
OUT="$1"; BASE="${2:-http://localhost:3000}"
CHROME="$HOME/Library/Caches/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-mac-arm64/chrome-headless-shell"
mkdir -p "$OUT"
for path in "" kontakt impressum datenschutz; do
  name="${path:-home}"
  for width in 1440 390; do
    "$CHROME" --headless --hide-scrollbars --disable-gpu --no-sandbox \
      --virtual-time-budget=8000 --window-size="$width,6000" \
      --screenshot="$OUT/$name-$width.png" "$BASE/$path" >/dev/null 2>&1
    echo "✓ $name-$width.png"
  done
done
EOF
chmod +x "$SCRATCH/shots.sh"
```

- [ ] **Step 2: Dev-Server starten (noch reines `next dev`)**

Run: Dev-Server-Befehl aus den Global Constraints.
Expected: `dev läuft`

- [ ] **Step 3: Baseline-Screenshots erzeugen**

Run: `zsh "$SCRATCH/shots.sh" "$SCRATCH/before"`
Expected: 8 Zeilen `✓ …png`. Danach `home-1440.png` und `home-390.png` mit dem Read-Tool ansehen: Hero, Sektionen und Footer müssen sichtbar sein (keine leeren, weißen Flächen durch nicht fertige Animationen). Falls Sektionen fehlen: `--virtual-time-budget=15000` setzen und wiederholen.

- [ ] **Step 4: Dev-Server stoppen, Build-Baseline prüfen**

Run: Stopp-Befehl, dann `npm run build 2>&1 | tail -15`
Expected: Build erfolgreich („✓ Generating static pages"), keine Fehler. Kein Commit nötig.

---

### Task 2: Tina-Grundgerüst + Einstellungen-Collection

**Files:**
- Modify: `package.json` (deps + scripts), `next.config.ts`, `.gitignore`, `tsconfig.json`
- Create: `tina/config.ts`, `tina/collections/settings.ts`, `content/settings/site.json`

**Interfaces:**
- Produces: generierter Client `tina/__generated__/client.ts` (Default-Export `client`, Aufruf `client.queries.settings({ relativePath: 'site.json' })` → `{ data: SettingsQuery, query: string, variables: { relativePath: string } }`), Typen in `tina/__generated__/types.ts` (`SettingsQuery`), Pfad-Alias `@tina/*`.

- [ ] **Step 1: Pakete installieren**

```bash
npm install tinacms@^3.12.1 && npm install -D @tinacms/cli@^2.6.1
```
Expected: ohne Peer-Dependency-Fehler (Tina 3 erlaubt `react >=16.14`).

- [ ] **Step 2: Scripts in `package.json` ersetzen**

```json
"scripts": {
  "dev": "tinacms dev -c \"next dev\"",
  "build": "tinacms build && next build",
  "build:local": "tinacms build --local -c \"next build\"",
  "start": "next start",
  "lint": "eslint"
}
```

- [ ] **Step 3: `next.config.ts` – Admin-Rewrite**

```ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  async rewrites() {
    return [{ source: '/admin', destination: '/admin/index.html' }]
  },
}

export default nextConfig
```

- [ ] **Step 4: `.gitignore` ergänzen (ans Ende)**

```
# tinacms (wird bei jedem Build generiert)
/tina/__generated__/
/public/admin/
```

- [ ] **Step 5: `tsconfig.json` – Alias `@tina/*`**

In `compilerOptions.paths`:
```json
"paths": {
  "@/*": ["./src/*"],
  "@tina/*": ["./tina/*"]
}
```

- [ ] **Step 6: Einstellungen-Collection anlegen – `tina/collections/settings.ts`**

```ts
import type { Collection } from 'tinacms'

export const settings: Collection = {
  name: 'settings',
  label: 'Einstellungen',
  path: 'content/settings',
  format: 'json',
  match: { include: 'site' },
  ui: {
    global: true,
    allowedActions: { create: false, delete: false },
    router: () => '/',
  },
  fields: [
    {
      type: 'object', name: 'contact', label: 'Kontakt', required: true,
      fields: [
        { type: 'string', name: 'phone', label: 'Telefonnummer (Anzeige)', required: true, description: 'z. B. +43 660 1083003' },
        { type: 'string', name: 'whatsapp', label: 'WhatsApp-Nummer (nur Ziffern inkl. Ländervorwahl)', required: true, description: 'z. B. 436601083003' },
        { type: 'string', name: 'email', label: 'E-Mail-Adresse', required: true },
      ],
    },
    {
      type: 'object', name: 'address', label: 'Adresse', required: true,
      fields: [
        { type: 'string', name: 'street', label: 'Straße & Hausnummer', required: true },
        { type: 'string', name: 'zip', label: 'PLZ', required: true },
        { type: 'string', name: 'city', label: 'Ort', required: true },
        { type: 'string', name: 'country', label: 'Land', required: true },
      ],
    },
    {
      type: 'object', name: 'company', label: 'Firmendaten', required: true,
      fields: [
        { type: 'string', name: 'legal', label: 'Firmenname (rechtlich)', required: true },
        { type: 'string', name: 'owner', label: 'Inhaber/in', required: true },
        { type: 'string', name: 'uid', label: 'UID-Nummer (leer lassen, wenn keine)' },
        { type: 'string', name: 'register', label: 'Firmenbuchnummer', required: true },
        { type: 'string', name: 'court', label: 'Firmenbuchgericht', required: true },
      ],
    },
    {
      type: 'object', name: 'google', label: 'Google-Bewertungen', required: true,
      fields: [
        { type: 'number', name: 'rating', label: 'Durchschnittsbewertung (z. B. 4.9)', required: true },
        { type: 'number', name: 'reviews', label: 'Anzahl Bewertungen', required: true },
        { type: 'string', name: 'mapsUrl', label: 'Link zum Google-Profil', required: true },
      ],
    },
    {
      type: 'object', name: 'seo', label: 'SEO', required: true,
      fields: [
        { type: 'string', name: 'siteName', label: 'Seitenname', required: true },
        { type: 'string', name: 'url', label: 'Website-URL (ohne Slash am Ende)', required: true },
        { type: 'string', name: 'defaultTitle', label: 'Titel der Startseite (Browser-Tab / Google)', required: true },
        { type: 'string', name: 'description', label: 'Beschreibung (Google-Snippet)', required: true, ui: { component: 'textarea' }, description: 'Platzhalter erlaubt: {phone}, {email}, {rating}, {reviews}' },
        { type: 'string', name: 'keywords', label: 'Keywords', list: true },
      ],
    },
    {
      type: 'object', name: 'footer', label: 'Footer', required: true,
      fields: [
        { type: 'string', name: 'description', label: 'Kurztext im Footer', required: true, ui: { component: 'textarea' } },
      ],
    },
    {
      type: 'object', name: 'design', label: 'Design', required: true,
      fields: [
        { type: 'string', name: 'accentColor', label: 'Akzentfarbe', required: true, ui: { component: 'color', colorFormat: 'hex' } },
        { type: 'image', name: 'logo', label: 'Logo (optional – ersetzt das Standard-Logo)' },
        { type: 'image', name: 'heroImage', label: 'Hintergrundbild Startseite', required: true },
      ],
    },
  ],
}
```
Hinweis: Falls TypeScript `colorFormat` im `ui`-Objekt ablehnt, das `ui`-Objekt des Farbfelds mit `as Collection['fields'][number]['ui']` casten – nicht das Feld entfernen.

- [ ] **Step 7: `tina/config.ts`**

```ts
import { defineConfig } from 'tinacms'
import { settings } from './collections/settings'

const branch =
  process.env.NEXT_PUBLIC_TINA_BRANCH ||
  process.env.VERCEL_GIT_COMMIT_REF ||
  process.env.HEAD ||
  'main'

export default defineConfig({
  branch,
  clientId: process.env.NEXT_PUBLIC_TINA_CLIENT_ID,
  token: process.env.TINA_TOKEN,
  build: { outputFolder: 'admin', publicFolder: 'public' },
  media: { tina: { mediaRoot: 'uploads', publicFolder: 'public' } },
  schema: { collections: [settings] },
})
```

- [ ] **Step 8: Inhalt `content/settings/site.json`**

```json
{
  "contact": {
    "phone": "+43 660 1083003",
    "whatsapp": "436601083003",
    "email": "info@taxigraz-gu.at"
  },
  "address": {
    "street": "Walter-Goldschmidt-Gasse 31",
    "zip": "8042",
    "city": "Graz",
    "country": "Österreich"
  },
  "company": {
    "legal": "Aktas Erol e.U.",
    "owner": "Erol Aktas",
    "uid": "",
    "register": "FN 523619 d",
    "court": "Landesgericht für ZRS Graz"
  },
  "google": {
    "rating": 4.9,
    "reviews": 673,
    "mapsUrl": "https://www.google.com/maps/place/Taxi+Graz+%2F%2FGU/"
  },
  "seo": {
    "siteName": "Taxi Graz GU",
    "url": "https://taxigraz-gu.at",
    "defaultTitle": "Taxi Graz | Taxi Graz GU - 24/7 Taxiservice, Flughafentransfer & Stretchlimousinen",
    "description": "Taxi Graz GU – Ihr 24/7 Taxiservice in Graz & Umgebung. Flughafentransfer, Stretchlimousinen, Botenfahrten. ☎ {phone}. {rating}★ Google ({reviews} Bewertungen). Jetzt anrufen!",
    "keywords": [
      "Taxi Graz", "Taxi Graz GU", "Taxi Graz Flughafen", "Taxi Graz Flughafen Preis", "Flughafentransfer Graz",
      "Taxi bestellen Graz", "Taxi Graz bestellen", "Stretchlimousine Graz", "Limousine mieten Graz", "Taxi Graz 24h",
      "Taxi Graz Nummer", "Taxi Graz Telefonnummer", "Taxi Graz Preise", "Taxi Graz Kosten", "Taxiunternehmen Graz",
      "Taxi Graz Umgebung", "Taxi Graz Andritz", "Taxi Graz Eggenberg", "Taxi Graz Nord", "Taxi Graz Don Bosco",
      "Taxi Feldkirchen bei Graz", "Taxi Kalsdorf bei Graz", "Taxi Seiersberg", "Taxi Gratkorn", "Botenfahrten Graz",
      "Günstigstes Taxi Graz"
    ]
  },
  "footer": {
    "description": "Ihr zuverlässiges Taxiunternehmen in Graz und Graz-Umgebung. 24/7 erreichbar für alle Ihre Fahrten."
  },
  "design": {
    "accentColor": "#E8B931",
    "heroImage": "/uploads/hero-taxi.jpg"
  }
}
```

- [ ] **Step 9: Hero-Bild in den Media-Ordner verschieben**

```bash
mkdir -p public/uploads && git mv public/images/hero-taxi.jpg public/uploads/hero-taxi.jpg
```
(Die Hero-Komponente zeigt bis Task 6 ein 404-Bild – bewusst, wird dort umgestellt.)

- [ ] **Step 10: Client generieren & lokal verifizieren**

Run: Dev-Server-Befehl, dann
```bash
ls tina/__generated__/client.ts tina/__generated__/types.ts tina/tina-lock.json
curl -s -X POST http://localhost:4001/graphql -H 'content-type: application/json' \
  -d '{"query":"{ settings(relativePath:\"site.json\"){ contact { phone } design { accentColor } seo { keywords } } }"}'
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/admin
```
Expected: alle drei Dateien vorhanden; GraphQL liefert `"phone":"+43 660 1083003"` und `"accentColor":"#E8B931"`; `/admin` → `200`. Dann Stopp-Befehl.

- [ ] **Step 11: Commit**

```bash
git add package.json package-lock.json next.config.ts .gitignore tsconfig.json tina/config.ts tina/collections/settings.ts tina/tina-lock.json content/settings/site.json public/uploads/hero-taxi.jpg
git commit -m "feat(cms): TinaCMS-Grundgerüst und Einstellungen-Collection"
```

---

### Task 3: Helper-Libs mit Tests (Farbe, Site-Helfer)

**Files:**
- Create: `vitest.config.ts`, `src/lib/color.ts`, `src/lib/site.ts`, `src/lib/__tests__/color.test.ts`, `src/lib/__tests__/site.test.ts`
- Modify: `package.json` (Script `test`)

**Interfaces:**
- Produces (`src/lib/color.ts`): `hexToHsl(hex: string): Hsl`, `hslToHex(hsl: Hsl): string` (Großbuchstaben, mit `#`), `deriveAccentPalette(hex: string): { base: string; light: string; dark: string }`, `accentCssVars(hex: string): Record<string, string>` mit Keys `--color-gold`, `--color-gold-light`, `--color-gold-dark`, `--accent`.
- Produces (`src/lib/site.ts`): `type SiteSettings = SettingsQuery['settings']`, `phoneRaw(phone: string): string`, `whatsappLink(whatsapp: string): string`, `interpolate(text: string, settings: SiteSettings): string`, `compact<T>(list): T[]`, `required<T>(value: T | null | undefined, name: string): T`.

- [ ] **Step 1: vitest installieren und konfigurieren**

```bash
npm install -D vitest
```
`package.json` → `"test": "vitest run"` zu den Scripts ergänzen.

`vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config'
import { fileURLToPath } from 'node:url'

export default defineConfig({
  test: { include: ['src/**/*.test.ts'] },
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
      '@tina': fileURLToPath(new URL('./tina', import.meta.url)),
    },
  },
})
```

- [ ] **Step 2: Failing Tests für `color.ts`**

`src/lib/__tests__/color.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { accentCssVars, deriveAccentPalette, hexToHsl, hslToHex } from '@/lib/color'

describe('hexToHsl / hslToHex', () => {
  it('wandelt reines Rot korrekt um', () => {
    expect(hexToHsl('#FF0000')).toEqual({ h: 0, s: 1, l: 0.5 })
    expect(hslToHex({ h: 0, s: 1, l: 0.5 })).toBe('#FF0000')
  })

  it('akzeptiert Kurzform und Kleinschreibung', () => {
    expect(hexToHsl('#fff')).toEqual({ h: 0, s: 0, l: 1 })
    expect(hslToHex({ h: 120, s: 1, l: 0.25 })).toBe('#008000')
  })

  it('Roundtrip bleibt innerhalb von ±1 pro Kanal', () => {
    const out = hslToHex(hexToHsl('#E8B931'))
    const channels = (hex: string) => [1, 3, 5].map((i) => parseInt(hex.slice(i, i + 2), 16))
    const [a, b] = [channels(out), channels('#E8B931')]
    a.forEach((v, i) => expect(Math.abs(v - b[i])).toBeLessThanOrEqual(1))
  })
})

describe('deriveAccentPalette', () => {
  it('liefert die Eingabe normalisiert als base', () => {
    expect(deriveAccentPalette('#e8b931').base).toBe('#E8B931')
  })

  it('hellt light auf und dunkelt dark ab, Farbton bleibt', () => {
    const p = deriveAccentPalette('#E8B931')
    const base = hexToHsl(p.base), light = hexToHsl(p.light), dark = hexToHsl(p.dark)
    expect(light.l).toBeGreaterThan(base.l)
    expect(dark.l).toBeLessThan(base.l)
    expect(Math.abs(light.h - base.h)).toBeLessThan(2)
    expect(Math.abs(dark.h - base.h)).toBeLessThan(2)
  })

  it('clamped an den Rändern', () => {
    expect(hexToHsl(deriveAccentPalette('#FFFFFF').light).l).toBeLessThanOrEqual(0.95)
    expect(hexToHsl(deriveAccentPalette('#000000').dark).l).toBeGreaterThanOrEqual(0.05)
  })
})

describe('accentCssVars', () => {
  it('setzt alle vier Variablen', () => {
    const vars = accentCssVars('#E8B931')
    expect(vars['--color-gold']).toBe('#E8B931')
    expect(vars['--accent']).toBe('#E8B931')
    expect(vars['--color-gold-light']).toMatch(/^#[0-9A-F]{6}$/)
    expect(vars['--color-gold-dark']).toMatch(/^#[0-9A-F]{6}$/)
  })
})
```

- [ ] **Step 3: Tests laufen lassen → müssen fehlschlagen**

Run: `npm test -- color`
Expected: FAIL (`Cannot find module '@/lib/color'`).

- [ ] **Step 4: `src/lib/color.ts` implementieren**

```ts
export interface Hsl {
  h: number // 0–360
  s: number // 0–1
  l: number // 0–1
}

function normalizeHex(hex: string): string {
  const clean = hex.trim().replace('#', '')
  const full = clean.length === 3 ? clean.split('').map((c) => c + c).join('') : clean
  return `#${full.toUpperCase()}`
}

export function hexToHsl(hex: string): Hsl {
  const full = normalizeHex(hex).slice(1)
  const r = parseInt(full.slice(0, 2), 16) / 255
  const g = parseInt(full.slice(2, 4), 16) / 255
  const b = parseInt(full.slice(4, 6), 16) / 255
  const max = Math.max(r, g, b)
  const min = Math.min(r, g, b)
  const l = (max + min) / 2
  if (max === min) return { h: 0, s: 0, l }
  const d = max - min
  const s = l > 0.5 ? d / (2 - max - min) : d / (max + min)
  let h: number
  if (max === r) h = (g - b) / d + (g < b ? 6 : 0)
  else if (max === g) h = (b - r) / d + 2
  else h = (r - g) / d + 4
  return { h: h * 60, s, l }
}

export function hslToHex({ h, s, l }: Hsl): string {
  const k = (n: number) => (n + h / 30) % 12
  const a = s * Math.min(l, 1 - l)
  const f = (n: number) => l - a * Math.max(-1, Math.min(k(n) - 3, Math.min(9 - k(n), 1)))
  const toHex = (v: number) => Math.round(v * 255).toString(16).padStart(2, '0')
  return `#${toHex(f(0))}${toHex(f(8))}${toHex(f(4))}`.toUpperCase()
}

export interface AccentPalette {
  base: string
  light: string
  dark: string
}

const clamp = (v: number, min: number, max: number) => Math.min(max, Math.max(min, v))

/** Leitet Hell-/Dunkel-Variante der Akzentfarbe ab (entspricht dem Verhältnis #E8B931 → #F5D668 / #C99B1D). */
export function deriveAccentPalette(hex: string): AccentPalette {
  const hsl = hexToHsl(hex)
  return {
    base: normalizeHex(hex),
    light: hslToHex({ ...hsl, l: clamp(hsl.l + 0.13, 0, 0.95) }),
    dark: hslToHex({ ...hsl, l: clamp(hsl.l - 0.1, 0.05, 1) }),
  }
}

/** CSS-Variablen für <html style> – Namen entsprechen globals.css. */
export function accentCssVars(hex: string): Record<string, string> {
  const p = deriveAccentPalette(hex)
  return {
    '--color-gold': p.base,
    '--color-gold-light': p.light,
    '--color-gold-dark': p.dark,
    '--accent': p.base,
  }
}
```

- [ ] **Step 5: Tests grün**

Run: `npm test -- color`
Expected: PASS (8 Tests).

- [ ] **Step 6: Failing Tests für `site.ts`**

`src/lib/__tests__/site.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { compact, interpolate, phoneRaw, required, whatsappLink, type SiteSettings } from '@/lib/site'

const settings = {
  contact: { phone: '+43 660 1083003', whatsapp: '436601083003', email: 'info@taxigraz-gu.at' },
  google: { rating: 4.9, reviews: 673, mapsUrl: 'https://maps.example' },
} as unknown as SiteSettings

describe('phoneRaw', () => {
  it('entfernt Leerzeichen und Sonderzeichen, behält +', () => {
    expect(phoneRaw('+43 660 1083003')).toBe('+436601083003')
    expect(phoneRaw('0316 / 12 34-56')).toBe('0316123456')
  })
})

describe('whatsappLink', () => {
  it('baut einen wa.me-Link nur aus Ziffern', () => {
    expect(whatsappLink('436601083003')).toBe('https://wa.me/436601083003')
    expect(whatsappLink('+43 660 1083003')).toBe('https://wa.me/436601083003')
  })
})

describe('interpolate', () => {
  it('ersetzt alle bekannten Platzhalter', () => {
    expect(interpolate('Ruf {phone} an oder schreib {email}. {rating}★ bei {reviews} Bewertungen.', settings)).toBe(
      'Ruf +43 660 1083003 an oder schreib info@taxigraz-gu.at. 4.9★ bei 673 Bewertungen.'
    )
  })
  it('lässt unbekannte Platzhalter stehen', () => {
    expect(interpolate('Hallo {name}', settings)).toBe('Hallo {name}')
  })
})

describe('compact', () => {
  it('filtert null/undefined und behandelt fehlende Listen', () => {
    expect(compact([1, null, 2, undefined])).toEqual([1, 2])
    expect(compact(null)).toEqual([])
    expect(compact(undefined)).toEqual([])
  })
})

describe('required', () => {
  it('gibt den Wert zurück oder wirft mit Namen', () => {
    expect(required('x', 'feld')).toBe('x')
    expect(() => required(null, 'home.hero')).toThrow('home.hero')
  })
})
```

- [ ] **Step 7: Test laufen lassen → FAIL**

Run: `npm test -- site`
Expected: FAIL (`Cannot find module '@/lib/site'`).

- [ ] **Step 8: `src/lib/site.ts` implementieren**

```ts
import type { SettingsQuery } from '@tina/__generated__/types'

export type SiteSettings = SettingsQuery['settings']

/** "+43 660 1083003" → "+436601083003" (für tel:-Links) */
export function phoneRaw(phone: string): string {
  return phone.replace(/[^\d+]/g, '')
}

/** WhatsApp-Nummer → https://wa.me/<nur Ziffern> */
export function whatsappLink(whatsapp: string): string {
  return `https://wa.me/${whatsapp.replace(/\D/g, '')}`
}

/** Ersetzt {phone}, {email}, {rating}, {reviews} in CMS-Texten. */
export function interpolate(text: string, settings: SiteSettings): string {
  const values: Record<string, string> = {
    phone: settings.contact.phone,
    email: settings.contact.email,
    rating: String(settings.google.rating),
    reviews: String(settings.google.reviews),
  }
  return text.replace(/\{(phone|email|rating|reviews)\}/g, (_match, key: string) => values[key])
}

/** Entfernt null/undefined aus Tina-Listen (GraphQL liefert Maybe<Array<Maybe<T>>>). */
export function compact<T>(list: ReadonlyArray<T | null | undefined> | null | undefined): T[] {
  return (list ?? []).filter((item): item is T => item !== null && item !== undefined)
}

/** Erzwingt Pflichtinhalte aus dem CMS mit sprechender Fehlermeldung. */
export function required<T>(value: T | null | undefined, name: string): T {
  if (value === null || value === undefined) {
    throw new Error(`CMS-Inhalt fehlt: ${name}`)
  }
  return value
}
```

- [ ] **Step 9: Alle Tests grün, Typen prüfen**

Run: `npm test && npx tsc --noEmit`
Expected: alle Tests PASS; `tsc` ohne Fehler (die Typen aus `tina/__generated__` existieren seit Task 2; falls nicht: `npx tinacms build --local` einmal ausführen).

- [ ] **Step 10: Commit**

```bash
git add vitest.config.ts package.json package-lock.json src/lib/color.ts src/lib/site.ts src/lib/__tests__
git commit -m "feat(cms): Farb- und Site-Helfer mit Tests"
```

---

### Task 4: Startseiten- und Kontakt-Collection mit Inhalten

**Files:**
- Create: `tina/collections/home.ts`, `tina/collections/kontakt.ts`, `content/pages/home.json`, `content/pages/kontakt.json`
- Modify: `tina/config.ts` (Collections registrieren)

**Interfaces:**
- Produces: `client.queries.home({ relativePath: 'home.json' })` → `HomeQuery` mit `home.{hero,trustBar,marquee,services,whyUs,pricing,serviceAreas,testimonials,faq,cta}`; `client.queries.kontakt({ relativePath: 'kontakt.json' })` → `KontaktQuery` mit `kontakt.{seo,hero,cards,form}`. Feldnamen exakt wie in den Schemas unten – Task 6/7 greifen darauf zu.

- [ ] **Step 1: `tina/collections/home.ts`**

```ts
import type { Collection, TinaField } from 'tinacms'

const SERVICE_ICONS = ['Plane', 'Crown', 'Package', 'Car', 'CalendarClock', 'Accessibility', 'MapPin', 'Clock', 'Shield', 'Star']
const AREA_ICONS = ['Building2', 'Trees', 'MapPin']

const str = (name: string, label: string, description?: string): TinaField => ({
  type: 'string', name, label, required: true, description,
})
const textarea = (name: string, label: string, description?: string): TinaField => ({
  type: 'string', name, label, required: true, description, ui: { component: 'textarea' },
})
const PLATZHALTER = 'Platzhalter erlaubt: {phone}, {email}, {rating}, {reviews}'

export const home: Collection = {
  name: 'home',
  label: 'Startseite',
  path: 'content/pages',
  format: 'json',
  match: { include: 'home' },
  ui: {
    allowedActions: { create: false, delete: false },
    router: () => '/',
  },
  fields: [
    {
      type: 'object', name: 'hero', label: 'Hero (oberster Bereich)', required: true,
      fields: [
        str('badge', 'Badge-Text', 'z. B. „Jetzt verfügbar"'),
        str('headline', 'Überschrift (Teil 1)', 'z. B. „Ihr Taxi in"'),
        str('headlineHighlight', 'Überschrift (hervorgehoben)', 'z. B. „Graz"'),
        str('subline', 'Unterzeile'),
        {
          type: 'rich-text', name: 'description', label: 'Beschreibung', required: true,
          overrides: { toolbar: ['bold'], showFloatingToolbar: false },
        },
        str('ctaCall', 'Button „Anrufen"'),
        str('ctaWhatsapp', 'Button „WhatsApp"'),
        str('cardTitle', 'Titel der Kontaktkarte'),
        str('scrollHint', 'Scroll-Hinweis'),
      ],
    },
    {
      type: 'object', name: 'trustBar', label: 'Vertrauensleiste', required: true,
      fields: [
        {
          type: 'object', name: 'items', label: 'Kacheln', list: true, required: true,
          ui: { itemProps: (item) => ({ label: item?.value }) },
          fields: [str('value', 'Wert (groß)', PLATZHALTER), str('label', 'Beschriftung', PLATZHALTER)],
        },
      ],
    },
    {
      type: 'object', name: 'marquee', label: 'Laufband', required: true,
      fields: [{ type: 'string', name: 'items', label: 'Begriffe', list: true, required: true }],
    },
    {
      type: 'object', name: 'services', label: 'Leistungen', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift (Teil 1)'),
        str('headingHighlight', 'Überschrift (hervorgehoben)'),
        textarea('intro', 'Einleitungstext'),
        {
          type: 'object', name: 'items', label: 'Leistungen', list: true, required: true,
          ui: { itemProps: (item) => ({ label: item?.title }) },
          fields: [
            { type: 'string', name: 'icon', label: 'Icon', required: true, options: SERVICE_ICONS },
            str('title', 'Titel'),
            textarea('description', 'Beschreibung'),
          ],
        },
      ],
    },
    {
      type: 'object', name: 'whyUs', label: 'Warum wir (Zahlen)', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift (Teil 1)'),
        str('headingHighlight', 'Überschrift (hervorgehoben)'),
        {
          type: 'object', name: 'stats', label: 'Zahlen', list: true, required: true,
          ui: { itemProps: (item) => ({ label: item?.label }) },
          fields: [
            { type: 'number', name: 'value', label: 'Zahl (wird hochgezählt)', required: true },
            { type: 'string', name: 'suffix', label: 'Anhang (z. B. „/7", „+", „ Min")' },
            str('label', 'Beschriftung'),
            str('sub', 'Zusatz', PLATZHALTER),
          ],
        },
        str('ctaLabel', 'Button-Text'),
        str('ctaWhatsappLabel', 'WhatsApp-Linktext'),
      ],
    },
    {
      type: 'object', name: 'pricing', label: 'Preise', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift'),
        textarea('text', 'Text'),
        str('ctaCall', 'Button „Anrufen"'),
        str('ctaWhatsapp', 'Button „WhatsApp"'),
        str('boxTitle', 'Titel der Vorteile-Box'),
        { type: 'string', name: 'benefits', label: 'Vorteile', list: true, required: true },
        str('boxCtaText', 'Text über der Telefonnummer'),
      ],
    },
    {
      type: 'object', name: 'serviceAreas', label: 'Service-Gebiete', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift'),
        textarea('intro', 'Einleitungstext'),
        {
          type: 'object', name: 'groups', label: 'Gebietsgruppen', list: true, required: true,
          ui: { itemProps: (item) => ({ label: item?.title }) },
          fields: [
            str('title', 'Gruppentitel'),
            { type: 'string', name: 'icon', label: 'Icon', required: true, options: AREA_ICONS },
            { type: 'string', name: 'areas', label: 'Orte / Routen', list: true, required: true },
          ],
        },
      ],
    },
    {
      type: 'object', name: 'testimonials', label: 'Kundenstimmen', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift (Teil 1)'),
        str('headingHighlight', 'Überschrift (hervorgehoben)'),
        textarea('intro', 'Einleitungstext', PLATZHALTER),
        {
          type: 'object', name: 'items', label: 'Bewertungen', list: true, required: true,
          ui: { itemProps: (item) => ({ label: item?.name }) },
          fields: [
            str('name', 'Name'),
            { type: 'number', name: 'rating', label: 'Sterne (1–5)', required: true },
            textarea('text', 'Text'),
            str('date', 'Datum (z. B. „vor 2 Monaten")'),
          ],
        },
        str('googleLinkLabel', 'Linktext zu Google', PLATZHALTER),
      ],
    },
    {
      type: 'object', name: 'faq', label: 'Häufige Fragen', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift'),
        {
          type: 'object', name: 'items', label: 'Fragen', list: true, required: true,
          ui: { itemProps: (item) => ({ label: item?.question }) },
          fields: [str('question', 'Frage'), textarea('answer', 'Antwort', PLATZHALTER)],
        },
      ],
    },
    {
      type: 'object', name: 'cta', label: 'Abschluss-Aufruf', required: true,
      fields: [
        str('eyebrow', 'Kleine Überschrift'),
        str('heading', 'Überschrift'),
        textarea('text', 'Text'),
        str('formLinkLabel', 'Linktext zum Kontaktformular'),
      ],
    },
  ],
}
```
Hinweis: Falls TypeScript `overrides` am rich-text-Feld ablehnt, `overrides` entfernen (Toolbar zeigt dann alle Optionen – funktional unkritisch).

- [ ] **Step 2: `tina/collections/kontakt.ts`**

```ts
import type { Collection, TinaField } from 'tinacms'

const str = (name: string, label: string): TinaField => ({ type: 'string', name, label, required: true })
const textarea = (name: string, label: string): TinaField => ({
  type: 'string', name, label, required: true, ui: { component: 'textarea' },
})

export const kontakt: Collection = {
  name: 'kontakt',
  label: 'Kontaktseite',
  path: 'content/pages',
  format: 'json',
  match: { include: 'kontakt' },
  ui: {
    allowedActions: { create: false, delete: false },
    router: () => '/kontakt',
  },
  fields: [
    {
      type: 'object', name: 'seo', label: 'SEO', required: true,
      fields: [str('title', 'Seitentitel'), textarea('description', 'Beschreibung (Google-Snippet)')],
    },
    {
      type: 'object', name: 'hero', label: 'Kopfbereich', required: true,
      fields: [str('eyebrow', 'Kleine Überschrift'), str('heading', 'Überschrift'), textarea('text', 'Text')],
    },
    {
      type: 'object', name: 'cards', label: 'Kontaktkarten', required: true,
      fields: [
        str('phoneSub', 'Zusatz unter Telefon'),
        str('whatsappSub', 'Zusatz unter WhatsApp'),
        str('hoursTitle', 'Titel Öffnungszeiten'),
        str('hoursValue', 'Öffnungszeiten'),
        str('hoursSub', 'Zusatz Öffnungszeiten'),
      ],
    },
    {
      type: 'object', name: 'form', label: 'Formular', required: true,
      fields: [str('heading', 'Überschrift'), textarea('text', 'Text')],
    },
  ],
}
```

- [ ] **Step 3: Collections in `tina/config.ts` registrieren**

```ts
import { home } from './collections/home'
import { kontakt } from './collections/kontakt'
// …
schema: { collections: [settings, home, kontakt] },
```

- [ ] **Step 4: `content/pages/home.json` – Inhalte 1:1 aus `src/data/*.ts` und den Sektionen**

```json
{
  "hero": {
    "badge": "Jetzt verfügbar",
    "headline": "Ihr Taxi in",
    "headlineHighlight": "Graz",
    "subline": "24/7 Taxiservice & Flughafentransfer",
    "description": {
      "type": "root",
      "children": [
        {
          "type": "p",
          "children": [
            { "type": "text", "text": "Flughafentransfer", "bold": true },
            { "type": "text", "text": ", " },
            { "type": "text", "text": "Stretchlimousinen", "bold": true },
            { "type": "text", "text": " & " },
            { "type": "text", "text": "zuverlässiger Taxiservice", "bold": true },
            { "type": "text", "text": " in Graz und Graz-Umgebung. Taxiunternehmen mit Festpreisen. Pünktlich, komfortabel und rund um die Uhr erreichbar." }
          ]
        }
      ]
    },
    "ctaCall": "Jetzt anrufen",
    "ctaWhatsapp": "WhatsApp",
    "cardTitle": "Sofort erreichbar",
    "scrollHint": "Mehr erfahren"
  },
  "trustBar": {
    "items": [
      { "value": "24/7", "label": "Taxi-Service Graz" },
      { "value": "{rating} ★", "label": "{reviews} Google Bewertungen" },
      { "value": "Seit 2010", "label": "Erfahrung & Vertrauen" },
      { "value": "100%", "label": "Barrierefrei" }
    ]
  },
  "marquee": {
    "items": [
      "Flughafentransfer Graz", "Stretchlimousinen", "Limousine mieten Graz", "Botenfahrten", "Taxi vorbestellen",
      "Barrierefreie Fahrten", "24/7 Service", "Kartenzahlung möglich", "WhatsApp Buchung", "Taxi Graz Andritz",
      "Taxi Graz Eggenberg", "Taxi Feldkirchen", "Taxi Kalsdorf", "Taxi Seiersberg", "Taxi Gratkorn"
    ]
  },
  "services": {
    "eyebrow": "Unsere Leistungen",
    "heading": "Taxi-Leistungen in",
    "headingHighlight": "Graz & Umgebung",
    "intro": "Von Flughafentransfers bis Stretchlimousinen: Taxi Graz GU ist Ihr zuverlässiger Partner für alle Fahrten in Graz und darüber hinaus.",
    "items": [
      { "icon": "Plane", "title": "Flughafentransfer", "description": "Zuverlässiger Transfer zum Flughafen Graz und zurück. Pünktliche Abholung garantiert – auch bei Frühflügen. Festpreise auf Anfrage." },
      { "icon": "Crown", "title": "Stretchlimousinen", "description": "Für besondere Anlässe bieten wir stilvolle Stretchlimousinen. Hochzeiten, Geburtstage oder Geschäftsfahrten – fahren Sie mit Klasse." },
      { "icon": "Package", "title": "Botenfahrten", "description": "Schnelle und zuverlässige Kurier- und Botenfahrten in Graz und Umgebung. Dokumente, Pakete und Medikamente sicher transportiert." },
      { "icon": "Car", "title": "Taxi-Service 24/7", "description": "Rund um die Uhr für Sie da. Ob Tag oder Nacht – rufen Sie an und wir bringen Sie sicher an Ihr Ziel. Komfortabel und pünktlich." },
      { "icon": "CalendarClock", "title": "Taxi vorbestellen", "description": "Planen Sie Ihre Fahrt im Voraus. Bestellen Sie Ihr Taxi bequem per Telefon oder WhatsApp vor – wir sind zur vereinbarten Zeit da." },
      { "icon": "Accessibility", "title": "Barrierefreie Fahrten", "description": "Rollstuhlgerechte Fahrzeuge für mobilitätseingeschränkte Fahrgäste. Barrierefreier Einstieg und professionelle Unterstützung." }
    ]
  },
  "whyUs": {
    "eyebrow": "Warum Taxi Graz GU",
    "heading": "Graz vertraut auf",
    "headingHighlight": "uns",
    "stats": [
      { "value": 24, "suffix": "/7", "label": "Erreichbar", "sub": "Tag & Nacht" },
      { "value": 673, "suffix": "+", "label": "Bewertungen", "sub": "{rating}★ auf Google" },
      { "value": 15, "suffix": "+", "label": "Jahre Erfahrung", "sub": "Seit 2010 in Graz" },
      { "value": 10, "suffix": " Min", "label": "Vor Ort", "sub": "Durchschnittliche Ankunft" }
    ],
    "ctaLabel": "Jetzt Taxi bestellen",
    "ctaWhatsappLabel": "per WhatsApp schreiben"
  },
  "pricing": {
    "eyebrow": "Taxi Preise Graz",
    "heading": "Faire Taxi-Tarife in Graz",
    "text": "Unsere Preise richten sich nach dem offiziellen Taxitarif der Steiermark. Für Flughafentransfers, Stretchlimousinen und Sonderfahrten bieten wir individuelle Festpreise an – rufen Sie uns an für ein unverbindliches Angebot.",
    "ctaCall": "Preis anfragen",
    "ctaWhatsapp": "Per WhatsApp anfragen",
    "boxTitle": "Ihre Vorteile bei Taxi Graz GU",
    "benefits": [
      "Transparente Festpreise auf Anfrage", "Keine versteckten Kosten", "Flughafentransfer zum Fixpreis",
      "Sonderkonditionen für Stammkunden", "Barzahlung & Kartenzahlung möglich", "Kostenlose Angebotserstellung"
    ],
    "boxCtaText": "Jetzt anrufen & individuelles Angebot erhalten"
  },
  "serviceAreas": {
    "eyebrow": "Service-Gebiete",
    "heading": "Taxi-Service in Graz und im gesamten Bezirk GU",
    "intro": "Taxi Graz GU ist Ihr lokaler Partner für Taxifahrten in Graz und Umgebung. Wir sind in folgenden Gebieten für Sie unterwegs:",
    "groups": [
      {
        "title": "Graz Stadt", "icon": "Building2",
        "areas": [
          "Taxi Graz", "Taxi Graz Andritz", "Taxi Graz Eggenberg", "Taxi Graz Nord", "Taxi Graz Don Bosco",
          "Taxi Graz St. Peter", "Taxi Graz Mariatrost", "Taxi Graz Liebenau", "Taxi Graz Jakomini", "Taxi Graz Lend",
          "Taxi Graz Gries", "Taxi Graz Gösting", "Taxi Graz Wetzelsdorf", "Taxi Graz Straßgang", "Taxi Graz Puntigam"
        ]
      },
      {
        "title": "Graz-Umgebung", "icon": "Trees",
        "areas": [
          "Taxi Feldkirchen bei Graz", "Taxi Kalsdorf bei Graz", "Taxi Seiersberg", "Taxi Gratkorn", "Taxi Hart bei Graz",
          "Taxi Hausmannstätten", "Taxi Raaba", "Taxi Laßnitzhöhe", "Taxi Premstätten", "Taxi Kumberg"
        ]
      },
      {
        "title": "Spezial-Routen", "icon": "MapPin",
        "areas": [
          "Taxi Graz Flughafen", "Flughafentransfer Graz", "Taxi Graz Hauptbahnhof", "Taxi Graz Umgebung",
          "Taxi Graz GU", "Taxi Graz 24h", "Taxi Steiermark"
        ]
      }
    ]
  },
  "testimonials": {
    "eyebrow": "Kundenstimmen",
    "heading": "Das sagen unsere Kunden über",
    "headingHighlight": "Taxi Graz GU",
    "intro": "Überzeugen Sie sich selbst: Über {reviews} zufriedene Kunden bewerten uns mit durchschnittlich {rating} Sternen auf Google.",
    "items": [
      { "name": "Lukas Sacher", "rating": 5, "text": "Danke für die Fahrt, sehr bequem, gute Unterhaltung, Bankomat Taxi... Top!", "date": "vor einem Monat" },
      { "name": "Eva Schultz", "rating": 5, "text": "Sehr freundlich und super Preis. Würden ihn wieder buchen :)", "date": "vor 3 Monaten" },
      { "name": "Lars Maschkow", "rating": 5, "text": "Spontan von der Straße aufgenommen, cooler Fahrer!", "date": "vor 2 Monaten" },
      { "name": "Christoph Wagner", "rating": 5, "text": "Super Service, pünktlich gekommen! Kann ich nur weiterempfehlen.", "date": "vor 2 Monaten" },
      { "name": "Ramona Koller", "rating": 5, "text": "Danke für den guten Service! Immer zuverlässig und freundlich.", "date": "vor einem Monat" },
      { "name": "Diyar Pekmezci", "rating": 5, "text": "Vielen Dank für die angenehme Fahrt. Sehr empfehlenswert!", "date": "vor 3 Monaten" }
    ],
    "googleLinkLabel": "Alle {reviews} Bewertungen auf Google ansehen"
  },
  "faq": {
    "eyebrow": "Häufige Fragen",
    "heading": "Häufig gestellte Fragen",
    "items": [
      { "question": "Wie bestelle ich am schnellsten ein Taxi in Graz?", "answer": "Am schnellsten erreichen Sie uns telefonisch unter {phone} – wir sind 24 Stunden am Tag, 7 Tage die Woche erreichbar. Alternativ können Sie uns auch per WhatsApp kontaktieren. In der Regel ist ein Taxi innerhalb weniger Minuten bei Ihnen." },
      { "question": "Kann ich bei Taxi Graz GU auch mit Karte bezahlen?", "answer": "Selbstverständlich. Unsere Fahrzeuge sind mit modernen Kartenterminals ausgestattet. Wir akzeptieren Bankomatkarte, Visa und Mastercard sowie kontaktlose Zahlung. Natürlich ist auch Barzahlung möglich." },
      { "question": "Was kostet ein Flughafentransfer zum Flughafen Graz?", "answer": "Für Flughafentransfers bieten wir transparente Festpreise an – so wissen Sie vorher genau, was die Fahrt kostet. Rufen Sie uns an unter {phone} für ein individuelles Angebot." },
      { "question": "Kann ich ein Taxi im Voraus bestellen?", "answer": "Ja, Sie können Ihr Taxi ganz bequem per Telefon oder WhatsApp vorbestellen. Geben Sie einfach das gewünschte Datum, die Uhrzeit und den Abholort an – wir sind pünktlich zur vereinbarten Zeit da." },
      { "question": "Bieten Sie auch Stretchlimousinen an?", "answer": "Ja! Für besondere Anlässe wie Hochzeiten, Geburtstage oder Geschäftsveranstaltungen bieten wir stilvolle Stretchlimousinen an. Kontaktieren Sie uns für ein individuelles Angebot." },
      { "question": "Sind Ihre Fahrzeuge barrierefrei?", "answer": "Ja, wir verfügen über rollstuhlgerechte Fahrzeuge mit barrierefreiem Eingang. Unsere Fahrer sind geschult und helfen Ihnen gerne beim Ein- und Aussteigen." },
      { "question": "Was kostet ein Taxi in Graz?", "answer": "Die Taxipreise in Graz richten sich nach dem offiziellen Steiermärkischen Taxitarif. Der Preis hängt von Strecke, Tageszeit und Zuschlägen ab. Für Festpreisangebote und genaue Preisauskunft rufen Sie uns einfach an unter {phone} – wir erstellen Ihnen gerne ein unverbindliches Angebot." },
      { "question": "Wie viel kostet ein Taxi zum Flughafen Graz?", "answer": "Der Preis für einen Flughafentransfer zum Flughafen Graz hängt vom Abholort ab. Wir bieten transparente Festpreise für Flughafentransfers an – rufen Sie uns an unter {phone} für Ihr individuelles Angebot." },
      { "question": "Kann ich eine Limousine oder Stretchlimousine in Graz mieten?", "answer": "Ja! Taxi Graz GU bietet stilvolle Stretchlimousinen für besondere Anlässe wie Hochzeiten, Geburtstage, JGA oder Geschäftsfahrten. Kontaktieren Sie uns für ein unverbindliches Angebot und Verfügbarkeit." },
      { "question": "Fahren Sie auch in die Graz-Umgebung und umliegende Gemeinden?", "answer": "Selbstverständlich! Wir bedienen ganz Graz und den gesamten Bezirk Graz-Umgebung, einschließlich Feldkirchen bei Graz, Kalsdorf, Seiersberg, Gratkorn, Hart bei Graz, Hausmannstätten, Raaba, Laßnitzhöhe und viele weitere Gemeinden." }
    ]
  },
  "cta": {
    "eyebrow": "Jetzt Taxi bestellen",
    "heading": "Bereit loszufahren?",
    "text": "Erreichen Sie uns jetzt – per Telefon, WhatsApp oder E-Mail. Wir sind 24/7 für Sie da.",
    "formLinkLabel": "Oder nutzen Sie unser Kontaktformular"
  }
}
```

- [ ] **Step 5: `content/pages/kontakt.json`**

```json
{
  "seo": {
    "title": "Kontakt",
    "description": "Kontaktieren Sie Taxi Graz GU – per Telefon, WhatsApp oder Kontaktformular. 24/7 erreichbar für Ihre Taxifahrt in Graz."
  },
  "hero": {
    "eyebrow": "Kontakt",
    "heading": "Sprechen Sie uns an",
    "text": "Ob per Telefon, WhatsApp oder Kontaktformular – wir sind 24 Stunden am Tag für Sie erreichbar."
  },
  "cards": {
    "phoneSub": "24/7 erreichbar",
    "whatsappSub": "Schnell & unkompliziert",
    "hoursTitle": "Öffnungszeiten",
    "hoursValue": "24 Stunden / 7 Tage",
    "hoursSub": "Auch an Feiertagen erreichbar"
  },
  "form": {
    "heading": "Schreiben Sie uns",
    "text": "Füllen Sie das Formular aus und wir melden uns schnellstmöglich bei Ihnen."
  }
}
```

- [ ] **Step 6: Verifizieren (GraphQL + Typen)**

Run: Dev-Server-Befehl, dann
```bash
curl -s -X POST http://localhost:4001/graphql -H 'content-type: application/json' \
  -d '{"query":"{ home(relativePath:\"home.json\"){ hero { headline description } services { items { icon title } } serviceAreas { groups { title areas } } faq { items { question } } } }"}' | head -c 600; echo
curl -s -X POST http://localhost:4001/graphql -H 'content-type: application/json' \
  -d '{"query":"{ kontakt(relativePath:\"kontakt.json\"){ hero { heading } cards { hoursValue } } }"}'; echo
grep -nE "^\s+(hero|trustBar|faq)[?]?: \{" tina/__generated__/types.ts | head -5
```
Expected: beide Queries liefern die Inhalte ohne `errors`; `description` kommt als Objekt mit `type: "root"`. Der `grep` zeigt, ob die Objektfelder als `hero: {` (non-null) oder `hero?: {` (Maybe) generiert werden – das Ergebnis notieren, Task 6 nutzt in beiden Fällen `required()`. Dann Stopp-Befehl.

- [ ] **Step 7: Commit**

```bash
git add tina/config.ts tina/collections/home.ts tina/collections/kontakt.ts tina/tina-lock.json content/pages/home.json content/pages/kontakt.json
git commit -m "feat(cms): Startseiten- und Kontakt-Collection mit migrierten Inhalten"
```

---

### Task 5: Layout, SiteShell, Settings-Context und globale Komponenten

**Files:**
- Create: `src/components/SettingsProvider.tsx`, `src/components/SiteShell.tsx`
- Modify: `src/app/layout.tsx`, `src/lib/metadata.ts`, `src/components/layout/Header.tsx`, `src/components/layout/Footer.tsx`, `src/components/sections/ContactBar.tsx`, `src/components/sections/FloatingContact.tsx`, `src/components/sections/StructuredData.tsx`, `src/components/ui/Logo.tsx`

**Interfaces:**
- Consumes: `client.queries.settings/home` (Task 2/4), `accentCssVars`, `interpolate`, `phoneRaw`, `whatsappLink`, `compact` (Task 3).
- Produces: `useSettings(): SiteSettings` (Client-Hook), `SiteShell({ settings: { data, query, variables }, children })`, `createMetadata(settings, { title, description, path?, noIndex? }): Metadata`, `StructuredData({ settings, faq, services })`. `src/lib/constants.ts` bleibt bis Task 9 bestehen (Sektionen nutzen es noch).

- [ ] **Step 1: `src/components/SettingsProvider.tsx`**

```tsx
'use client'

import { createContext, useContext } from 'react'
import type { SiteSettings } from '@/lib/site'

const SettingsContext = createContext<SiteSettings | null>(null)

export function SettingsProvider({ settings, children }: { settings: SiteSettings; children: React.ReactNode }) {
  return <SettingsContext.Provider value={settings}>{children}</SettingsContext.Provider>
}

/** Globale Einstellungen (Kontakt, Firma, Google, SEO, Design) – live im Visual Editing. */
export function useSettings(): SiteSettings {
  const settings = useContext(SettingsContext)
  if (!settings) throw new Error('useSettings() muss innerhalb von <SettingsProvider> verwendet werden')
  return settings
}
```

- [ ] **Step 2: `src/components/SiteShell.tsx`**

```tsx
'use client'

import { useEffect } from 'react'
import { useTina } from 'tinacms/dist/react'
import type { SettingsQuery } from '@tina/__generated__/types'
import { SettingsProvider } from '@/components/SettingsProvider'
import { accentCssVars } from '@/lib/color'
import { ContactBar } from '@/components/sections/ContactBar'
import { Header } from '@/components/layout/Header'
import { Footer } from '@/components/layout/Footer'
import { FloatingContact } from '@/components/sections/FloatingContact'
import { BackToTop } from '@/components/ui/BackToTop'
import { CookieConsentProvider } from '@/components/ui/CookieConsent'

export interface SiteShellProps {
  settings: { data: SettingsQuery; query: string; variables: { relativePath: string } }
  children: React.ReactNode
}

export function SiteShell({ settings, children }: SiteShellProps) {
  const { data } = useTina(settings)
  const site = data.settings

  // SSR setzt die Variablen bereits inline auf <html>; hier nur die Live-Vorschau im Visual Editing.
  useEffect(() => {
    const vars = accentCssVars(site.design.accentColor)
    for (const [name, value] of Object.entries(vars)) {
      document.documentElement.style.setProperty(name, value)
    }
  }, [site.design.accentColor])

  return (
    <SettingsProvider settings={site}>
      <CookieConsentProvider>
        <ContactBar />
        <Header />
        <main>{children}</main>
        <Footer />
        <FloatingContact />
        <BackToTop />
      </CookieConsentProvider>
    </SettingsProvider>
  )
}
```

- [ ] **Step 3: `src/lib/metadata.ts` auf Einstellungen umstellen**

```ts
import { type Metadata } from 'next'
import type { SiteSettings } from './site'

export function createMetadata(
  settings: SiteSettings,
  { title, description, path = '', noIndex = false }: { title: string; description: string; path?: string; noIndex?: boolean }
): Metadata {
  const fullTitle = `${title} | ${settings.seo.siteName}`
  const url = `${settings.seo.url}${path}`

  return {
    title: fullTitle,
    description,
    ...(noIndex && { robots: { index: false, follow: false } }),
    openGraph: { title: fullTitle, description, url, siteName: settings.seo.siteName, type: 'website', locale: 'de_AT' },
    twitter: { card: 'summary_large_image', title: fullTitle, description },
    alternates: { canonical: url },
  }
}
```

- [ ] **Step 4: `src/components/sections/StructuredData.tsx` auf Props umstellen**

```tsx
import type { SiteSettings } from '@/lib/site'
import { interpolate } from '@/lib/site'

export interface StructuredDataProps {
  settings: SiteSettings
  faq: { question: string; answer: string }[]
  services: { title: string; description: string }[]
}

function JsonLd({ data }: { data: object }) {
  return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }} />
}

export function StructuredData({ settings, faq, services }: StructuredDataProps) {
  const s = settings
  const localBusiness = {
    '@context': 'https://schema.org',
    '@type': 'TaxiService',
    name: s.seo.siteName,
    alternateName: 'Taxi Graz //GU',
    description: interpolate(s.seo.description, s),
    url: s.seo.url,
    telephone: s.contact.phone,
    email: s.contact.email,
    address: {
      '@type': 'PostalAddress',
      streetAddress: s.address.street,
      addressLocality: s.address.city,
      postalCode: s.address.zip,
      addressCountry: 'AT',
    },
    geo: { '@type': 'GeoCoordinates', latitude: 47.0500761, longitude: 15.4743797 },
    areaServed: [
      { '@type': 'City', name: 'Graz' },
      { '@type': 'AdministrativeArea', name: 'Graz-Umgebung' },
      { '@type': 'Place', name: 'Flughafen Graz' },
    ],
    openingHoursSpecification: {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
      opens: '00:00',
      closes: '23:59',
    },
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: s.google.rating,
      reviewCount: s.google.reviews,
      bestRating: 5,
      worstRating: 1,
    },
    priceRange: '€€',
    paymentAccepted: 'Cash, Credit Card, Debit Card',
    currenciesAccepted: 'EUR',
    sameAs: [s.google.mapsUrl],
    hasOfferCatalog: {
      '@type': 'OfferCatalog',
      name: 'Taxi-Leistungen',
      itemListElement: services.map((service) => ({
        '@type': 'Offer',
        itemOffered: { '@type': 'Service', name: service.title, description: service.description },
      })),
    },
  }

  const faqPage = {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faq.map((item) => ({
      '@type': 'Question',
      name: item.question,
      acceptedAnswer: { '@type': 'Answer', text: interpolate(item.answer, s) },
    })),
  }

  const breadcrumbs = {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Startseite', item: s.seo.url },
      { '@type': 'ListItem', position: 2, name: 'Kontakt', item: `${s.seo.url}/kontakt` },
      { '@type': 'ListItem', position: 3, name: 'Impressum', item: `${s.seo.url}/impressum` },
      { '@type': 'ListItem', position: 4, name: 'Datenschutz', item: `${s.seo.url}/datenschutz` },
    ],
  }

  return (
    <>
      <JsonLd data={localBusiness} />
      <JsonLd data={faqPage} />
      <JsonLd data={breadcrumbs} />
    </>
  )
}
```

- [ ] **Step 5: `src/app/layout.tsx` komplett ersetzen**

```tsx
import type { Metadata } from 'next'
import client from '@tina/__generated__/client'
import { dmSerifDisplay, outfit } from '@/lib/fonts'
import { accentCssVars } from '@/lib/color'
import { compact, interpolate } from '@/lib/site'
import { SiteShell } from '@/components/SiteShell'
import { StructuredData } from '@/components/sections/StructuredData'
import './globals.css'

const loadSettings = () => client.queries.settings({ relativePath: 'site.json' })
const loadHome = () => client.queries.home({ relativePath: 'home.json' })

export async function generateMetadata(): Promise<Metadata> {
  const { data } = await loadSettings()
  const s = data.settings
  const description = interpolate(s.seo.description, s)
  return {
    metadataBase: new URL(s.seo.url),
    title: { default: s.seo.defaultTitle, template: `%s | ${s.seo.siteName}` },
    description,
    keywords: compact(s.seo.keywords),
    openGraph: {
      title: s.seo.defaultTitle,
      description,
      url: s.seo.url,
      siteName: s.seo.siteName,
      locale: 'de_AT',
      type: 'website',
    },
    twitter: { card: 'summary_large_image', title: s.seo.defaultTitle, description },
    robots: {
      index: true,
      follow: true,
      googleBot: { index: true, follow: true, 'max-snippet': -1, 'max-image-preview': 'large', 'max-video-preview': -1 },
    },
    alternates: { canonical: s.seo.url },
  }
}

export default async function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  const [settings, home] = await Promise.all([loadSettings(), loadHome()])
  const site = settings.data.settings

  return (
    <html
      lang="de"
      className={`${dmSerifDisplay.variable} ${outfit.variable}`}
      style={accentCssVars(site.design.accentColor) as React.CSSProperties}
    >
      <head>
        <StructuredData
          settings={site}
          faq={compact(home.data.home.faq.items)}
          services={compact(home.data.home.services.items)}
        />
      </head>
      <body className="font-[var(--font-body)] antialiased bg-[var(--color-cream)]">
        <SiteShell settings={{ data: settings.data, query: settings.query, variables: settings.variables }}>
          {children}
        </SiteShell>
      </body>
    </html>
  )
}
```
Falls `home.data.home.faq` laut Task 4 Step 6 als Maybe generiert wird: `compact(home.data.home.faq?.items)` bzw. `services?.items`.

- [ ] **Step 6: `src/components/ui/Logo.tsx` – Akzentfarbe + optionales Upload-Logo**

Komplett ersetzen; im SVG **alle 11** `#E8B931` durch `var(--color-gold)` ersetzen (Pfad-Daten unverändert aus der bestehenden Datei übernehmen):
```tsx
'use client'

import Image from 'next/image'
import { useSettings } from '@/components/SettingsProvider'

export function Logo({ className }: { className?: string }) {
  const { design, seo } = useSettings()

  if (design.logo) {
    return <Image src={design.logo} alt={seo.siteName} width={360} height={140} unoptimized className={className} />
  }

  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 360 140" fill="none" className={className}>
      {/* Äußerer Ring */}
      <circle cx="70" cy="70" r="66" stroke="var(--color-gold)" strokeWidth="2.5" fill="none" />
      <circle cx="70" cy="70" r="60" stroke="var(--color-gold)" strokeWidth="0.5" fill="none" opacity="0.25" />
      {/* Taxi-Silhouette – <path d="…"> unverändert aus der bisherigen Datei, nur fill="var(--color-gold)" */}
      <g transform="translate(50, 22) scale(0.32)">
        <path fill="var(--color-gold)" fillRule="evenodd" clipRule="evenodd" d="…BESTEHENDER PFAD…" />
      </g>
      <text x="70" y="105" fontFamily="var(--font-body), sans-serif" fontSize="50" fontWeight="700" fill="var(--color-gold)" textAnchor="middle" letterSpacing="10">GU</text>
      <text x="160" y="50" fontFamily="var(--font-body), sans-serif" fontSize="40" fontWeight="700" fill="var(--color-gold)">TAXI</text>
      <text x="160" y="80" fontFamily="var(--font-body), sans-serif" fontSize="17" fontWeight="300" fill="var(--color-gold)" letterSpacing="5" opacity="0.65">GRAZ &amp; GU</text>
      <line x1="160" y1="92" x2="306" y2="92" stroke="var(--color-gold)" strokeWidth="0.7" opacity="0.3" />
      <text x="160" y="112" fontFamily="var(--font-body), sans-serif" fontSize="10" fontWeight="400" fill="var(--color-gold)" letterSpacing="3.5" opacity="0.4">24/7 PREMIUM SERVICE</text>
    </svg>
  )
}
```
Prüfen: `grep -c "E8B931" src/components/ui/Logo.tsx` → `0`.

- [ ] **Step 7: `Header.tsx`, `Footer.tsx`, `ContactBar.tsx`, `FloatingContact.tsx` auf `useSettings` umstellen**

Für alle vier Dateien gilt dieselbe Ersetzungstabelle (Markup/Klassen unverändert):

| bisher | neu |
|---|---|
| `import { SITE_CONFIG } from '@/lib/constants'` | `import { useSettings } from '@/components/SettingsProvider'` + `import { phoneRaw, whatsappLink } from '@/lib/site'` + `import { tinaField } from 'tinacms/dist/react'` |
| erste Zeile im Komponentenrumpf | `const settings = useSettings()` |
| `SITE_CONFIG.phoneRaw` | `phoneRaw(settings.contact.phone)` |
| `SITE_CONFIG.phone` | `settings.contact.phone` |
| `SITE_CONFIG.whatsappLink` | `whatsappLink(settings.contact.whatsapp)` |
| `SITE_CONFIG.email` | `settings.contact.email` |
| `SITE_CONFIG.google.rating` / `.reviews` / `.mapsUrl` | `settings.google.rating` / `.reviews` / `.mapsUrl` |
| `SITE_CONFIG.address.street` / `.zip` / `.city` | `settings.address.street` / `.zip` / `.city` |
| `SITE_CONFIG.company.legal` | `settings.company.legal` |

Zusätzlich:
- `Footer.tsx`: `'use client'` als erste Zeile ergänzen; ungenutzten Import `Phone` entfernen; der Absatz unter dem Logo bekommt `data-tina-field={tinaField(settings.footer, 'description')}` und rendert `{settings.footer.description}` statt des Fließtexts.
- `ContactBar.tsx`: `'use client'` als erste Zeile ergänzen.
- `Header.tsx`: ungenutzten Import `usePathname` entfernen; auf dem `<span className="hidden sm:inline">` mit der Telefonnummer `data-tina-field={tinaField(settings.contact, 'phone')}`.
- `FloatingContact.tsx`: nur die Tabelle anwenden.

Prüfen: `grep -rn "SITE_CONFIG" src/components/layout src/components/sections/ContactBar.tsx src/components/sections/FloatingContact.tsx src/components/ui` → keine Treffer.

- [ ] **Step 8: Verifizieren**

Run: `npx tsc --noEmit && npm run lint 2>&1 | tail -3`
Expected: `tsc` ohne Fehler; Lint ≤ 1 Error (CookieConsent, vorbestehend), Warnings ≤ 1 (nur noch `Link` in datenschutz).

Run: Dev-Server-Befehl, dann
```bash
curl -s http://localhost:3000/ | grep -o 'application/ld+json' | wc -l
curl -s http://localhost:3000/ | grep -o '+43 660 1083003' | head -1
curl -s http://localhost:3000/ | grep -o 'style="--color-gold:[^"]*"'
```
Expected: `3`, die Telefonnummer, und `style="--color-gold:#E8B931;--color-gold-light:…;--color-gold-dark:…;--accent:#E8B931"`. Dann Stopp-Befehl.

- [ ] **Step 9: Commit**

```bash
git add src/components/SettingsProvider.tsx src/components/SiteShell.tsx src/app/layout.tsx src/lib/metadata.ts src/components/layout src/components/sections/ContactBar.tsx src/components/sections/FloatingContact.tsx src/components/sections/StructuredData.tsx src/components/ui/Logo.tsx
git commit -m "feat(cms): Layout, SiteShell und globale Komponenten auf CMS-Einstellungen"
```

---

### Task 6: Startseite auf CMS-Daten (HomeClient + 10 Sektionen)

**Files:**
- Create: `src/app/HomeClient.tsx`, `src/lib/styles.ts`
- Modify: `src/app/page.tsx`, `src/components/sections/{Hero,TrustBar,Marquee,Services,WhyUs,Pricing,ServiceAreas,Testimonials,FAQ,CTA}.tsx`, `src/types/index.ts`
- Delete: `src/data/faq.ts`, `src/data/features.ts`, `src/data/marquee.ts`, `src/data/serviceAreas.ts`, `src/data/testimonials.ts` (`src/data/navigation.ts` bleibt – Navigation ist nicht im CMS)

**Interfaces:**
- Consumes: `HomeQuery` (Task 4), `useSettings` (Task 5), `compact/required/interpolate/phoneRaw/whatsappLink` (Task 3).
- Produces: jede Sektion exportiert `export function X({ data }: { data: XData })` mit `type XData = NonNullable<HomeQuery['home']['x']>`; `goldGridStyle` in `src/lib/styles.ts`.

**Regel für alle Sektionen:** Bestehendes JSX kopieren, **nur** Text-/Datenstellen ersetzen und `data-tina-field` ergänzen. Klassen, framer-motion-Props, Struktur bleiben identisch.

- [ ] **Step 1: `src/lib/styles.ts` – Grid-Muster in Akzentfarbe**

```ts
/** Ersetzt das bisherige rgba(232,185,49,0.3)-Raster – folgt der Akzentfarbe. */
const gridLine = 'color-mix(in srgb, var(--color-gold) 30%, transparent)'

export const goldGridStyle: React.CSSProperties = {
  backgroundImage: `linear-gradient(${gridLine} 1px, transparent 1px), linear-gradient(90deg, ${gridLine} 1px, transparent 1px)`,
  backgroundSize: '80px 80px',
}
```

- [ ] **Step 2: `src/app/page.tsx` und `src/app/HomeClient.tsx`**

`src/app/page.tsx`:
```tsx
import client from '@tina/__generated__/client'
import { HomeClient } from './HomeClient'

export default async function HomePage() {
  const res = await client.queries.home({ relativePath: 'home.json' })
  return <HomeClient data={res.data} query={res.query} variables={res.variables} />
}
```

`src/app/HomeClient.tsx`:
```tsx
'use client'

import { useTina } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { required } from '@/lib/site'
import { Hero } from '@/components/sections/Hero'
import { TrustBar } from '@/components/sections/TrustBar'
import { Marquee } from '@/components/sections/Marquee'
import { Services } from '@/components/sections/Services'
import { WhyUs } from '@/components/sections/WhyUs'
import { Pricing } from '@/components/sections/Pricing'
import { ServiceAreas } from '@/components/sections/ServiceAreas'
import { Testimonials } from '@/components/sections/Testimonials'
import { FAQ } from '@/components/sections/FAQ'
import { CTA } from '@/components/sections/CTA'

export interface HomeClientProps {
  data: HomeQuery
  query: string
  variables: { relativePath: string }
}

export function HomeClient(props: HomeClientProps) {
  const { data } = useTina(props)
  const home = data.home

  return (
    <>
      <Hero data={required(home.hero, 'home.hero')} />
      <TrustBar data={required(home.trustBar, 'home.trustBar')} />
      <Marquee data={required(home.marquee, 'home.marquee')} />
      <Services data={required(home.services, 'home.services')} />
      <WhyUs data={required(home.whyUs, 'home.whyUs')} />
      <Pricing data={required(home.pricing, 'home.pricing')} />
      <ServiceAreas data={required(home.serviceAreas, 'home.serviceAreas')} />
      <Testimonials data={required(home.testimonials, 'home.testimonials')} />
      <FAQ data={required(home.faq, 'home.faq')} />
      <CTA data={required(home.cta, 'home.cta')} />
    </>
  )
}
```

- [ ] **Step 3: `Hero.tsx`**

Kopf der Datei:
```tsx
'use client'

import Image from 'next/image'
import { Phone, MessageCircle, Mail, ArrowDown, Star } from 'lucide-react'
import { motion } from 'framer-motion'
import { tinaField } from 'tinacms/dist/react'
import { TinaMarkdown, type TinaMarkdownContent } from 'tinacms/dist/rich-text'
import type { HomeQuery } from '@tina/__generated__/types'
import { useSettings } from '@/components/SettingsProvider'
import { phoneRaw, whatsappLink } from '@/lib/site'
import { goldGridStyle } from '@/lib/styles'

export type HeroData = NonNullable<HomeQuery['home']['hero']>

// Beschreibung liegt als Rich-Text vor; <p> wird zu Fragment, damit sie im bestehenden <motion.p> bleibt.
const inlineMarkdown = {
  p: (props: { children?: React.ReactNode }) => <>{props.children}</>,
  bold: (props: { children?: React.ReactNode }) => <strong className="text-white">{props.children}</strong>,
}

export function Hero({ data }: { data: HeroData }) {
  const settings = useSettings()
  const tel = `tel:${phoneRaw(settings.contact.phone)}`
  const wa = whatsappLink(settings.contact.whatsapp)
```
Ersetzungen im JSX (Rest 1:1 wie bisher):
| bisher | neu |
|---|---|
| `src="/images/hero-taxi.jpg"` | `src={settings.design.heroImage}` + `data-tina-field={tinaField(settings.design, 'heroImage')}` auf dem umgebenden `<div className="absolute inset-0 z-0">` |
| `style={{ backgroundImage: 'linear-gradient(rgba(232,185,49,0.3)…', backgroundSize: '80px 80px' }}` | `style={goldGridStyle}` |
| `Jetzt verfügbar` | `<span data-tina-field={tinaField(data, 'badge')}>{data.badge}</span>` (innerhalb des bestehenden `<span>`, nach dem grünen Punkt) |
| `{SITE_CONFIG.google.rating} ({SITE_CONFIG.google.reviews} Bewertungen)` | `{settings.google.rating} ({settings.google.reviews} Bewertungen)` |
| `<h1 …>Ihr Taxi<br />in{' '}<span …>Graz…` | `<h1 … data-tina-field={tinaField(data, 'headline')}>{data.headline.split(' ').slice(0, -1).join(' ')}<br />{data.headline.split(' ').slice(-1)[0]}{' '}<span … data-tina-field={tinaField(data, 'headlineHighlight')}>{data.headlineHighlight}` – der letzte Wortteil („in") steht nach dem `<br />`, wie bisher |
| `24/7 Taxiservice &amp; Flughafentransfer` | `<p … data-tina-field={tinaField(data, 'subline')}>{data.subline}</p>` |
| Inhalt des `<motion.p className="mt-8 max-w-lg …">` | `<TinaMarkdown content={data.description as TinaMarkdownContent} components={inlineMarkdown} />` + `data-tina-field={tinaField(data, 'description')}` auf dem `<motion.p>` |
| `href={\`tel:${SITE_CONFIG.phoneRaw}\`}` (3×) | `href={tel}` |
| `href={SITE_CONFIG.whatsappLink}` (2×) | `href={wa}` |
| `Jetzt anrufen` | `<span className="relative z-10" data-tina-field={tinaField(data, 'ctaCall')}>{data.ctaCall}</span>` |
| `WhatsApp` (Button-Text) | `<span data-tina-field={tinaField(data, 'ctaWhatsapp')}>{data.ctaWhatsapp}</span>` |
| `Sofort erreichbar` | `<h2 … data-tina-field={tinaField(data, 'cardTitle')}>{data.cardTitle}</h2>` |
| `{SITE_CONFIG.phone}` | `<span data-tina-field={tinaField(settings.contact, 'phone')}>{settings.contact.phone}</span>` |
| `{SITE_CONFIG.email}` (2×) | `{settings.contact.email}` |
| `{SITE_CONFIG.google.rating}` / `({SITE_CONFIG.google.reviews} Google-Bewertungen)` | `{settings.google.rating}` / `({settings.google.reviews} Google-Bewertungen)` |
| `Mehr erfahren` | `<span … data-tina-field={tinaField(data, 'scrollHint')}>{data.scrollHint}</span>` |

- [ ] **Step 4: `TrustBar.tsx`**

```tsx
'use client'

import { motion } from 'framer-motion'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { useSettings } from '@/components/SettingsProvider'
import { compact, interpolate } from '@/lib/site'

export type TrustBarData = NonNullable<HomeQuery['home']['trustBar']>

export function TrustBar({ data }: { data: TrustBarData }) {
  const settings = useSettings()
  const items = compact(data.items)

  return (
    <section className="relative border-y border-[var(--color-border)] bg-white">
      <div className="mx-auto grid max-w-7xl grid-cols-2 divide-x divide-[var(--color-border)] lg:grid-cols-4">
        {items.map((item, i) => (
          <motion.div
            key={i}
            data-tina-field={tinaField(item)}
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5, delay: i * 0.1 }}
            className="flex flex-col items-center gap-1 px-4 py-8 text-center md:py-10"
          >
            <span className="text-2xl font-bold tracking-tight text-[var(--color-black)] md:text-3xl">
              {interpolate(item.value, settings)}
            </span>
            <span className="text-xs font-medium text-[var(--color-gray-400)] md:text-sm">
              {interpolate(item.label, settings)}
            </span>
          </motion.div>
        ))}
      </div>
    </section>
  )
}
```

- [ ] **Step 5: `Marquee.tsx`**

```tsx
import { Car } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { compact } from '@/lib/site'

export type MarqueeData = NonNullable<HomeQuery['home']['marquee']>

export function Marquee({ data }: { data: MarqueeData }) {
  const items = compact(data.items)

  return (
    <section className="relative overflow-hidden bg-[var(--color-gold)] py-4" data-tina-field={tinaField(data, 'items')}>
      {/* Gradient masks for smooth fade */}
      <div className="absolute left-0 top-0 z-10 h-full w-20 bg-gradient-to-r from-[var(--color-gold)] to-transparent" />
      <div className="absolute right-0 top-0 z-10 h-full w-20 bg-gradient-to-l from-[var(--color-gold)] to-transparent" />

      <div className="animate-marquee flex whitespace-nowrap">
        {/* Double the items for seamless loop */}
        {[...items, ...items].map((item, i) => (
          <div key={i} className="mx-6 flex items-center gap-3 shrink-0">
            <Car className="h-4 w-4 text-[var(--color-black)]/40" />
            <span className="text-sm font-bold uppercase tracking-[0.15em] text-[var(--color-black)]">
              {item}
            </span>
          </div>
        ))}
      </div>
    </section>
  )
}
```

- [ ] **Step 6: `Services.tsx`**

Kopf ersetzen:
```tsx
'use client'

import { motion } from 'framer-motion'
import { Plane, Crown, Package, Car, CalendarClock, Accessibility, MapPin, Clock, Shield, Star, type LucideIcon } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { Section } from '@/components/layout/Section'
import { Card } from '@/components/ui'
import { compact } from '@/lib/site'

export type ServicesData = NonNullable<HomeQuery['home']['services']>

const iconMap: Record<string, LucideIcon> = { Plane, Crown, Package, Car, CalendarClock, Accessibility, MapPin, Clock, Shield, Star }

export function Services({ data }: { data: ServicesData }) {
  const items = compact(data.items)
```
Ersetzungen im JSX:
| bisher | neu |
|---|---|
| `Unsere Leistungen` | `<motion.span … data-tina-field={tinaField(data, 'eyebrow')}>{data.eyebrow}</motion.span>` |
| `Taxi-Leistungen in{' '}<span …>Graz &amp; Umgebung</span>` | `<motion.h2 … data-tina-field={tinaField(data, 'heading')}>{data.heading}{' '}<span className="text-[var(--color-gold-dark)]" data-tina-field={tinaField(data, 'headingHighlight')}>{data.headingHighlight}</span></motion.h2>` |
| Intro-Absatz | `<motion.p … data-tina-field={tinaField(data, 'intro')}>{data.intro}</motion.p>` |
| `{features.map((feature, i) => {` | `{items.map((feature, i) => {` |
| `<motion.div key={feature.title}` | `<motion.div key={i} data-tina-field={tinaField(feature)}` |

- [ ] **Step 7: `WhyUs.tsx`**

Kopf (AnimatedNumber bleibt unverändert):
```tsx
'use client'

import { motion, useMotionValue, useTransform, animate } from 'framer-motion'
import { useEffect, useRef, useState } from 'react'
import { Phone } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { useSettings } from '@/components/SettingsProvider'
import { compact, interpolate, phoneRaw, whatsappLink } from '@/lib/site'
import { goldGridStyle } from '@/lib/styles'

export type WhyUsData = NonNullable<HomeQuery['home']['whyUs']>
```
`const stats = […]` löschen. Signatur: `export function WhyUs({ data }: { data: WhyUsData }) { const settings = useSettings(); const stats = compact(data.stats)`.
Ersetzungen:
| bisher | neu |
|---|---|
| Grid-`style={{ backgroundImage: … }}` | `style={goldGridStyle}` |
| `Warum Taxi Graz GU` | `<span … data-tina-field={tinaField(data, 'eyebrow')}>{data.eyebrow}</span>` |
| `Graz vertraut auf{' '}<span …>uns<svg…` | `<h2 … data-tina-field={tinaField(data, 'heading')}>{data.heading}{' '}<span className="relative inline-block text-[var(--color-gold)]" data-tina-field={tinaField(data, 'headingHighlight')}>{data.headingHighlight}<svg …` (SVG unverändert) |
| `<motion.div key={stat.label}` | `<motion.div key={i} data-tina-field={tinaField(stat)}` |
| `<AnimatedNumber value={stat.value} suffix={stat.suffix} />` | `<AnimatedNumber value={stat.value} suffix={stat.suffix ?? ''} />` |
| `{stat.sub}` | `{interpolate(stat.sub, settings)}` |
| `href={\`tel:${SITE_CONFIG.phoneRaw}\`}` | `href={\`tel:${phoneRaw(settings.contact.phone)}\`}` |
| `Jetzt Taxi bestellen` | `<span className="relative z-10" data-tina-field={tinaField(data, 'ctaLabel')}>{data.ctaLabel}</span>` |
| `href={SITE_CONFIG.whatsappLink}` | `href={whatsappLink(settings.contact.whatsapp)}` |
| `per WhatsApp schreiben` | `<span data-tina-field={tinaField(data, 'ctaWhatsappLabel')}>{data.ctaWhatsappLabel}</span>` |

- [ ] **Step 8: Zwischenstand prüfen (noch ohne Pricing…CTA)**

Run: `npx tsc --noEmit 2>&1 | grep -E "Hero|TrustBar|Marquee|Services|WhyUs|HomeClient|page.tsx" | head`
Expected: keine Fehler in diesen Dateien (Fehler in Pricing/ServiceAreas/Testimonials/FAQ/CTA sind bis Step 13 erwartet, weil HomeClient ihnen bereits `data` übergibt).

- [ ] **Step 9: `Pricing.tsx`**

Kopf:
```tsx
'use client'

import { motion } from 'framer-motion'
import { Phone, Check, MessageCircle } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { Section } from '@/components/layout/Section'
import { useSettings } from '@/components/SettingsProvider'
import { compact, phoneRaw, whatsappLink } from '@/lib/site'

export type PricingData = NonNullable<HomeQuery['home']['pricing']>

export function Pricing({ data }: { data: PricingData }) {
  const settings = useSettings()
  const benefits = compact(data.benefits)
  const tel = `tel:${phoneRaw(settings.contact.phone)}`
```
`const benefits = […]` (Modul-Ebene) löschen. Ersetzungen:
| bisher | neu |
|---|---|
| `Taxi Preise Graz` | `<motion.span … data-tina-field={tinaField(data, 'eyebrow')}>{data.eyebrow}</motion.span>` |
| `Faire Taxi-Tarife in Graz` | `<motion.h2 … data-tina-field={tinaField(data, 'heading')}>{data.heading}</motion.h2>` |
| Text-Absatz | `<motion.p … data-tina-field={tinaField(data, 'text')}>{data.text}</motion.p>` |
| `href={\`tel:${SITE_CONFIG.phoneRaw}\`}` (2×) | `href={tel}` |
| `Preis anfragen` | `<span data-tina-field={tinaField(data, 'ctaCall')}>{data.ctaCall}</span>` |
| `href={SITE_CONFIG.whatsappLink}` | `href={whatsappLink(settings.contact.whatsapp)}` |
| `Per WhatsApp anfragen` | `<span data-tina-field={tinaField(data, 'ctaWhatsapp')}>{data.ctaWhatsapp}</span>` |
| `Ihre Vorteile bei Taxi Graz GU` | `<h3 … data-tina-field={tinaField(data, 'boxTitle')}>{data.boxTitle}</h3>` |
| `<div className="mt-6 space-y-4">` | `<div className="mt-6 space-y-4" data-tina-field={tinaField(data, 'benefits')}>` |
| `Jetzt anrufen &amp; individuelles Angebot erhalten` | `<p … data-tina-field={tinaField(data, 'boxCtaText')}>{data.boxCtaText}</p>` |
| `{SITE_CONFIG.phone}` | `{settings.contact.phone}` |

- [ ] **Step 10: `ServiceAreas.tsx` – Gruppen aus dem CMS statt Index-Slicing**

```tsx
'use client'

import { motion } from 'framer-motion'
import { MapPin, Building2, Trees, type LucideIcon } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { Section } from '@/components/layout/Section'
import { compact } from '@/lib/site'

export type ServiceAreasData = NonNullable<HomeQuery['home']['serviceAreas']>
type AreaGroupData = NonNullable<NonNullable<ServiceAreasData['groups']>[number]>

const iconMap: Record<string, LucideIcon> = { Building2, Trees, MapPin }

function AreaGroup({ group, delay }: { group: AreaGroupData; delay: number }) {
  const Icon = iconMap[group.icon] ?? MapPin
  const areas = compact(group.areas)
  return (
    <motion.div
      data-tina-field={tinaField(group)}
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ delay }}
    >
      <div className="mb-4 flex items-center justify-center gap-2">
        <Icon className="h-4 w-4 text-[var(--color-gold-dark)]" />
        <span className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--color-gray-500)]">
          {group.title}
        </span>
      </div>
      <div className="flex flex-wrap justify-center gap-2">
        {areas.map((area) => (
          <span
            key={area}
            className="group inline-flex items-center gap-1.5 rounded-full border border-[var(--color-border)] bg-white px-4 py-2 text-sm font-medium text-[var(--color-gray-600)] transition-all duration-300 hover:border-[var(--color-gold)]/50 hover:bg-[var(--color-gold)]/5 hover:text-[var(--color-black)]"
          >
            <MapPin className="h-3.5 w-3.5 text-[var(--color-gold)] transition-transform group-hover:scale-110" />
            {area}
          </span>
        ))}
      </div>
    </motion.div>
  )
}

export function ServiceAreas({ data }: { data: ServiceAreasData }) {
  const groups = compact(data.groups)
  return (
    <Section id="gebiete" className="bg-[var(--color-gray-50)]">
      <div className="text-center">
        <motion.span
          data-tina-field={tinaField(data, 'eyebrow')}
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          viewport={{ once: true }}
          className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--color-gold-dark)]"
        >
          {data.eyebrow}
        </motion.span>
        <motion.h2
          data-tina-field={tinaField(data, 'heading')}
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ delay: 0.1 }}
          className="mt-3 font-[var(--font-display)] text-3xl font-bold tracking-tight text-[var(--color-black)] sm:text-4xl"
        >
          {data.heading}
        </motion.h2>
        <motion.p
          data-tina-field={tinaField(data, 'intro')}
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ delay: 0.2 }}
          className="mx-auto mt-4 max-w-2xl text-[var(--color-gray-500)]"
        >
          {data.intro}
        </motion.p>
      </div>

      <div className="mt-12 space-y-10">
        {groups.map((group, i) => (
          <AreaGroup key={i} group={group} delay={0.3 + i * 0.1} />
        ))}
      </div>
    </Section>
  )
}
```

- [ ] **Step 11: `Testimonials.tsx`**

Kopf:
```tsx
'use client'

import { motion } from 'framer-motion'
import { Star } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { Section } from '@/components/layout/Section'
import { Card } from '@/components/ui'
import { useSettings } from '@/components/SettingsProvider'
import { compact, interpolate } from '@/lib/site'

export type TestimonialsData = NonNullable<HomeQuery['home']['testimonials']>

export function Testimonials({ data }: { data: TestimonialsData }) {
  const settings = useSettings()
  const items = compact(data.items)
```
Ersetzungen:
| bisher | neu |
|---|---|
| `Kundenstimmen` | `<motion.span … data-tina-field={tinaField(data, 'eyebrow')}>{data.eyebrow}</motion.span>` |
| `Das sagen unsere Kunden über{' '}<span …>Taxi Graz GU</span>` | `<motion.h2 … data-tina-field={tinaField(data, 'heading')}>{data.heading}{' '}<span className="text-[var(--color-gold-dark)]" data-tina-field={tinaField(data, 'headingHighlight')}>{data.headingHighlight}</span></motion.h2>` |
| Intro-Absatz mit `{SITE_CONFIG.google.reviews}` … | `<motion.p … data-tina-field={tinaField(data, 'intro')}>{interpolate(data.intro, settings)}</motion.p>` |
| `{testimonials.map((testimonial, i) => (` | `{items.map((testimonial, i) => (` |
| `<motion.div key={i}` (Karte) | `<motion.div key={i} data-tina-field={tinaField(testimonial)}` |
| `href={SITE_CONFIG.google.mapsUrl}` | `href={settings.google.mapsUrl}` |
| `Alle {SITE_CONFIG.google.reviews} Bewertungen auf Google ansehen` | `<span data-tina-field={tinaField(data, 'googleLinkLabel')}>{interpolate(data.googleLinkLabel, settings)}</span>` |
Das Google-SVG mit den vier Marken-Hexfarben bleibt unverändert (Google-Logo, keine Akzentfarbe).

- [ ] **Step 12: `FAQ.tsx` und `CTA.tsx`**

`FAQ.tsx`:
```tsx
'use client'

import { motion } from 'framer-motion'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { Section } from '@/components/layout/Section'
import { Accordion } from '@/components/ui'
import { useSettings } from '@/components/SettingsProvider'
import { compact, interpolate } from '@/lib/site'

export type FaqData = NonNullable<HomeQuery['home']['faq']>

export function FAQ({ data }: { data: FaqData }) {
  const settings = useSettings()
  const items = compact(data.items).map((item) => ({
    question: item.question,
    answer: interpolate(item.answer, settings),
  }))

  return (
    <Section id="faq" className="bg-[var(--color-gray-50)]" narrow>
      <div className="text-center">
        <motion.span
          data-tina-field={tinaField(data, 'eyebrow')}
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          viewport={{ once: true }}
          className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--color-gold-dark)]"
        >
          {data.eyebrow}
        </motion.span>
        <motion.h2
          data-tina-field={tinaField(data, 'heading')}
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ delay: 0.1 }}
          className="mt-3 font-[var(--font-display)] text-3xl font-bold tracking-tight text-[var(--color-black)] sm:text-4xl"
        >
          {data.heading}
        </motion.h2>
      </div>

      <motion.div
        data-tina-field={tinaField(data, 'items')}
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ delay: 0.2 }}
        className="mt-12 rounded-2xl border border-[var(--color-border)] bg-white px-6 md:px-8"
      >
        <Accordion items={items} />
      </motion.div>
    </Section>
  )
}
```

`CTA.tsx` – Kopf:
```tsx
'use client'

import { motion } from 'framer-motion'
import { Phone, MessageCircle, Mail, ArrowRight } from 'lucide-react'
import { tinaField } from 'tinacms/dist/react'
import type { HomeQuery } from '@tina/__generated__/types'
import { Container } from '@/components/layout/Container'
import { useSettings } from '@/components/SettingsProvider'
import { phoneRaw, whatsappLink } from '@/lib/site'

export type CtaData = NonNullable<HomeQuery['home']['cta']>

export function CTA({ data }: { data: CtaData }) {
  const settings = useSettings()
```
Ersetzungen:
| bisher | neu |
|---|---|
| `Jetzt Taxi bestellen` (eyebrow) | `<motion.span … data-tina-field={tinaField(data, 'eyebrow')}>{data.eyebrow}</motion.span>` |
| `Bereit loszufahren?` | `<motion.h2 … data-tina-field={tinaField(data, 'heading')}>{data.heading}</motion.h2>` |
| Text-Absatz | `<motion.p … data-tina-field={tinaField(data, 'text')}>{data.text}</motion.p>` |
| `href={\`tel:${SITE_CONFIG.phoneRaw}\`}` | `href={\`tel:${phoneRaw(settings.contact.phone)}\`}` |
| `{SITE_CONFIG.phone}` | `{settings.contact.phone}` |
| `href={SITE_CONFIG.whatsappLink}` | `href={whatsappLink(settings.contact.whatsapp)}` |
| `{SITE_CONFIG.email}` (2×) | `{settings.contact.email}` |
| `Oder nutzen Sie unser Kontaktformular` | `<span data-tina-field={tinaField(data, 'formLinkLabel')}>{data.formLinkLabel}</span>` |

- [ ] **Step 13: Alte Datenquellen entfernen**

```bash
git rm src/data/faq.ts src/data/features.ts src/data/marquee.ts src/data/serviceAreas.ts src/data/testimonials.ts
grep -rn "@/data/\(faq\|features\|marquee\|serviceAreas\|testimonials\)" src || echo "keine Referenzen mehr"
grep -rn "SITE_CONFIG" src/components/sections src/app/page.tsx src/app/HomeClient.tsx || echo "keine SITE_CONFIG in Sektionen"
```
`src/types/index.ts` auf `NavItem` reduzieren:
```ts
export interface NavItem {
  label: string
  href: string
}
```

- [ ] **Step 14: Verifizieren – Typen, Lint, Rendering, Screenshots**

Run: `npx tsc --noEmit && npm run lint 2>&1 | tail -3`
Expected: `tsc` sauber; Lint ≤ 1 Error (CookieConsent), ≤ 1 Warning.

Run: Dev-Server-Befehl, dann
```bash
curl -s http://localhost:3000/ | grep -c "data-tina-field"
curl -s http://localhost:3000/ | grep -o "Ihr Taxi\|Taxi-Leistungen in\|Graz vertraut auf\|Faire Taxi-Tarife\|Bereit loszufahren" | sort -u
curl -s http://localhost:3000/ | grep -o "unter +43 660 1083003" | head -1
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/uploads/hero-taxi.jpg
zsh "$SCRATCH/shots.sh" "$SCRATCH/task6"
magick compare -metric AE "$SCRATCH/before/home-1440.png" "$SCRATCH/task6/home-1440.png" "$SCRATCH/task6/home-1440-diff.png" 2>&1; echo
```
Expected: ≥ 40 `data-tina-field`; alle fünf Überschriften; die interpolierte Telefonnummer in einer FAQ-Antwort; `200` fürs Hero-Bild. Screenshot-Vergleich: `home-1440.png` (vorher/nachher) mit dem Read-Tool nebeneinander ansehen – Layout, Abstände, Farben identisch (Pixel-Differenzen nur durch Marquee-Position/Animations-Timing). Dann Stopp-Befehl.

- [ ] **Step 15: Commit**

```bash
git add -A src/app/page.tsx src/app/HomeClient.tsx src/lib/styles.ts src/components/sections src/types/index.ts src/data
git commit -m "feat(cms): Startseite auf CMS-Inhalte mit Visual Editing umgestellt"
```

---

### Task 7: Kontaktseite mit Visual Editing

**Files:**
- Create: `src/app/kontakt/KontaktClient.tsx`
- Modify: `src/app/kontakt/page.tsx`

**Interfaces:**
- Consumes: `KontaktQuery` (Task 4), `createMetadata(settings, …)` (Task 5), `useSettings`, `goldGridStyle`.
- Produces: `KontaktClient({ data, query, variables })`.

- [ ] **Step 1: `src/app/kontakt/page.tsx` ersetzen**

```tsx
import type { Metadata } from 'next'
import client from '@tina/__generated__/client'
import { createMetadata } from '@/lib/metadata'
import { KontaktClient } from './KontaktClient'

const RELATIVE_PATH = 'kontakt.json'

export async function generateMetadata(): Promise<Metadata> {
  const [settings, page] = await Promise.all([
    client.queries.settings({ relativePath: 'site.json' }),
    client.queries.kontakt({ relativePath: RELATIVE_PATH }),
  ])
  return createMetadata(settings.data.settings, {
    title: page.data.kontakt.seo.title,
    description: page.data.kontakt.seo.description,
    path: '/kontakt',
  })
}

export default async function KontaktPage() {
  const res = await client.queries.kontakt({ relativePath: RELATIVE_PATH })
  return <KontaktClient data={res.data} query={res.query} variables={res.variables} />
}
```

- [ ] **Step 2: `src/app/kontakt/KontaktClient.tsx`**

Das bisherige JSX aus `page.tsx` (ab `<section className="relative overflow-hidden bg-[var(--color-gray-900)] pt-40 pb-16">` bis `</>`), mit diesen Ersetzungen; Rahmen:
```tsx
'use client'

import { useTina, tinaField } from 'tinacms/dist/react'
import type { KontaktQuery } from '@tina/__generated__/types'
import { Phone, MessageCircle, Mail, MapPin, Clock } from 'lucide-react'
import { Section } from '@/components/layout/Section'
import { ContactForm } from '@/components/sections/ContactForm'
import { useSettings } from '@/components/SettingsProvider'
import { phoneRaw, whatsappLink } from '@/lib/site'
import { goldGridStyle } from '@/lib/styles'
import { GoogleMap } from './GoogleMap'

export interface KontaktClientProps {
  data: KontaktQuery
  query: string
  variables: { relativePath: string }
}

export function KontaktClient(props: KontaktClientProps) {
  const { data } = useTina(props)
  const page = data.kontakt
  const settings = useSettings()

  return (
    <>
      {/* … bisheriges JSX … */}
    </>
  )
}
```
| bisher | neu |
|---|---|
| Grid-`style={{ backgroundImage: … }}` | `style={goldGridStyle}` |
| `Kontakt` (eyebrow) | `<span … data-tina-field={tinaField(page.hero, 'eyebrow')}>{page.hero.eyebrow}</span>` |
| `Sprechen Sie uns an` | `<h1 … data-tina-field={tinaField(page.hero, 'heading')}>{page.hero.heading}</h1>` |
| Hero-Absatz | `<p … data-tina-field={tinaField(page.hero, 'text')}>{page.hero.text}</p>` |
| `href={\`tel:${SITE_CONFIG.phoneRaw}\`}` | `href={\`tel:${phoneRaw(settings.contact.phone)}\`}` |
| `{SITE_CONFIG.phone}` | `<span data-tina-field={tinaField(settings.contact, 'phone')}>{settings.contact.phone}</span>` |
| `24/7 erreichbar` | `<p … data-tina-field={tinaField(page.cards, 'phoneSub')}>{page.cards.phoneSub}</p>` |
| `href={SITE_CONFIG.whatsappLink}` | `href={whatsappLink(settings.contact.whatsapp)}` |
| `Schnell & unkompliziert` | `<p … data-tina-field={tinaField(page.cards, 'whatsappSub')}>{page.cards.whatsappSub}</p>` |
| `{SITE_CONFIG.email}` (2×) | `{settings.contact.email}` |
| `{SITE_CONFIG.address.street}` / `.zip` / `.city` | `{settings.address.street}` / `.zip` / `.city` |
| `Öffnungszeiten` | `<h3 … data-tina-field={tinaField(page.cards, 'hoursTitle')}>{page.cards.hoursTitle}</h3>` |
| `24 Stunden / 7 Tage` | `<p … data-tina-field={tinaField(page.cards, 'hoursValue')}>{page.cards.hoursValue}</p>` |
| `Auch an Feiertagen erreichbar` | `<p … data-tina-field={tinaField(page.cards, 'hoursSub')}>{page.cards.hoursSub}</p>` |
| `Schreiben Sie uns` | `<h2 … data-tina-field={tinaField(page.form, 'heading')}>{page.form.heading}</h2>` |
| Formular-Absatz | `<p … data-tina-field={tinaField(page.form, 'text')}>{page.form.text}</p>` |

Falls `page.hero`/`cards`/`form` laut Task 4 Step 6 als Maybe generiert werden: oben `const hero = required(page.hero, 'kontakt.hero')` usw. (Import `required` aus `@/lib/site`) und entsprechend `hero.eyebrow` verwenden.

- [ ] **Step 3: Verifizieren**

Run: `npx tsc --noEmit && npm run lint 2>&1 | tail -2`, dann Dev-Server-Befehl und
```bash
curl -s http://localhost:3000/kontakt | grep -o "Sprechen Sie uns an\|24 Stunden / 7 Tage\|Schreiben Sie uns" | sort -u
curl -s http://localhost:3000/kontakt | grep -o '<title>[^<]*</title>'
zsh "$SCRATCH/shots.sh" "$SCRATCH/task7"
```
Expected: alle drei Texte; `<title>Kontakt | Taxi Graz GU</title>`; `kontakt-1440.png` vorher/nachher per Read-Tool vergleichen – identisch. Stopp-Befehl.

- [ ] **Step 4: Commit**

```bash
git add src/app/kontakt/page.tsx src/app/kontakt/KontaktClient.tsx
git commit -m "feat(cms): Kontaktseite auf CMS-Inhalte umgestellt"
```

---

### Task 8: SEO-Routen (OG-Image, Sitemap, Robots) auf Einstellungen

**Files:**
- Modify: `src/app/opengraph-image.tsx`, `src/app/sitemap.ts`, `src/app/robots.ts`

**Interfaces:**
- Consumes: `client.queries.settings`, `deriveAccentPalette`.

- [ ] **Step 1: `src/app/sitemap.ts`**

```ts
import type { MetadataRoute } from 'next'
import client from '@tina/__generated__/client'

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const { data } = await client.queries.settings({ relativePath: 'site.json' })
  const baseUrl = data.settings.seo.url
  const now = new Date()

  return [
    { url: baseUrl, lastModified: now, changeFrequency: 'weekly', priority: 1 },
    { url: `${baseUrl}/kontakt`, lastModified: now, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${baseUrl}/impressum`, lastModified: now, changeFrequency: 'yearly', priority: 0.3 },
    { url: `${baseUrl}/datenschutz`, lastModified: now, changeFrequency: 'yearly', priority: 0.3 },
  ]
}
```

- [ ] **Step 2: `src/app/robots.ts`**

```ts
import type { MetadataRoute } from 'next'
import client from '@tina/__generated__/client'

export default async function robots(): Promise<MetadataRoute.Robots> {
  const { data } = await client.queries.settings({ relativePath: 'site.json' })
  return {
    rules: [{ userAgent: '*', allow: '/', disallow: ['/api/', '/admin'] }],
    sitemap: `${data.settings.seo.url}/sitemap.xml`,
  }
}
```

- [ ] **Step 3: `src/app/opengraph-image.tsx`**

`export const runtime = 'edge'` **entfernen** (das Bild wird beim Build statisch erzeugt). Funktion wird `async`, Werte kommen aus den Einstellungen; Layout/Größen bleiben identisch:
```tsx
import { ImageResponse } from 'next/og'
import client from '@tina/__generated__/client'
import { deriveAccentPalette } from '@/lib/color'

export const alt = 'Taxi Graz GU – 24/7 Taxiservice in Graz & Umgebung'
export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'

export default async function OGImage() {
  const { data } = await client.queries.settings({ relativePath: 'site.json' })
  const s = data.settings
  const gold = deriveAccentPalette(s.design.accentColor).base
  const host = s.seo.url.replace(/^https?:\/\//, '')

  return new ImageResponse(
    (
      /* bisheriges JSX mit folgenden Ersetzungen:
         '#E8B931'                 → gold  (alle 7 Stellen)
         4.9                       → {s.google.rating}
         (673 Bewertungen)         → ({s.google.reviews} Bewertungen)
         ☎ +43 660 1083003         → ☎ {s.contact.phone}
         taxigraz-gu.at            → {host}
         Übrige Hexwerte (#0A0A0A, #FFFFFF, #FAFAF8, #6E6E66, #4A4A44) bleiben. */
    ),
    { ...size }
  )
}
```

- [ ] **Step 4: Verifizieren**

Run: `npx tsc --noEmit`, dann Dev-Server-Befehl und
```bash
curl -s http://localhost:3000/sitemap.xml | grep -c "<loc>"
curl -s http://localhost:3000/robots.txt
curl -s -o "$SCRATCH/og.png" -w '%{http_code} %{content_type}\n' http://localhost:3000/opengraph-image
```
Expected: `4`; robots mit `Sitemap: https://taxigraz-gu.at/sitemap.xml` und `Disallow: /admin`; `200 image/png` – `og.png` per Read-Tool ansehen (goldene Leiste, Telefonnummer, Rating). Stopp-Befehl.

- [ ] **Step 5: Commit**

```bash
git add src/app/opengraph-image.tsx src/app/sitemap.ts src/app/robots.ts
git commit -m "feat(cms): OG-Image, Sitemap und Robots aus CMS-Einstellungen"
```

---

### Task 9: Rechtsseiten als Rich-Text (Legal-Collection, MDX, LegalClient)

**Files:**
- Create: `tina/collections/legal.ts`, `content/legal/impressum.mdx`, `content/legal/datenschutz.mdx`, `src/components/legal/LegalClient.tsx`, `src/components/legal/CompanyBlock.tsx`, `src/components/legal/markdownComponents.tsx`
- Modify: `tina/config.ts`, `src/app/impressum/page.tsx`, `src/app/datenschutz/page.tsx`
- Delete: `src/lib/constants.ts`

**Interfaces:**
- Consumes: `useSettings`, `createMetadata`, `phoneRaw`.
- Produces: `client.queries.legal({ relativePath: 'impressum.mdx' | 'datenschutz.mdx' })` → `LegalQuery` mit `legal.{title,intro,seoDescription,showCompanyBlock,body}`; `LegalClient({ data, query, variables })`.

- [ ] **Step 1: `tina/collections/legal.ts` + Registrierung**

```ts
import type { Collection } from 'tinacms'

export const legal: Collection = {
  name: 'legal',
  label: 'Rechtliches',
  path: 'content/legal',
  format: 'mdx',
  ui: {
    allowedActions: { create: false, delete: false },
    router: ({ document }) => `/${document._sys.filename}`,
  },
  fields: [
    { type: 'string', name: 'title', label: 'Titel', isTitle: true, required: true },
    { type: 'string', name: 'intro', label: 'Untertitel im Kopfbereich', required: true },
    { type: 'string', name: 'seoDescription', label: 'SEO-Beschreibung', required: true },
    { type: 'boolean', name: 'showCompanyBlock', label: 'Firmenblock aus den Einstellungen oben anzeigen' },
    {
      type: 'rich-text', name: 'body', label: 'Inhalt', isBody: true,
      overrides: { toolbar: ['heading', 'bold', 'italic', 'link', 'ul', 'ol'], headingLevels: ['h2', 'h3'] },
    },
  ],
}
```
In `tina/config.ts`: `import { legal } from './collections/legal'` und `collections: [settings, home, kontakt, legal]`.

- [ ] **Step 2: `content/legal/impressum.mdx`**

Frontmatter + Body. Body = alle Abschnitte aus `src/app/impressum/page.tsx` **außer** „Informationspflicht laut § 5 ECG", „Kontakt", „UID-Nummer" und „Firmenbuchnummer & Firmenbuchgericht" (die rendert `CompanyBlock`). Konvertierungsregeln: `<h2>` → `## `, `<h3>` → `### `, `<p>` → Absatz (Leerzeile dazwischen), Zeilen innerhalb eines `space-y-1`-Blocks → Absatz mit zwei Leerzeichen am Zeilenende (harter Umbruch), `<ul><li>` → `- `, `<strong>` → `**…**`, `<a href="…">Text</a>` → `[Text](…)`, `&amp;` → `&`. `{SITE_CONFIG.…}`-Werte im Body als Klartext eintragen (Aktas Erol e.U., Walter-Goldschmidt-Gasse 31, 8042 Graz). Anfang der Datei:
```mdx
---
title: Impressum
intro: Angaben gemäß § 5 ECG und § 25 MedienG.
seoDescription: Impressum von Taxi Graz GU
showCompanyBlock: true
---

## Unternehmensgegenstand

Personenbeförderung mit PKW (Taxigewerbe)

## Aufsichtsbehörde / Gewerbebehörde

Bezirkshauptmannschaft Graz-Umgebung

## Berufsbezeichnung & Berufsrecht

Berufsbezeichnung: Taxiunternehmer  
Verleihungsstaat: Österreich  
Anwendbare Rechtsvorschriften: Gewerbeordnung 1994 (GewO), Gelegenheitsverkehrsgesetz (GelverkG), Betriebsordnung für den nichtlinienmäßigen Personenverkehr (BO 1994)  
Nachzulesen unter: [ris.bka.gv.at](https://www.ris.bka.gv.at)

## Kammerzugehörigkeit

…
```
`intro` = der bisherige `<p className="mt-3 …">`-Text unter dem H1 (aus der Datei übernehmen). Weiter bis „Webdesign & Umsetzung" (Link zu unicorn-factory.net bleibt).

- [ ] **Step 3: `content/legal/datenschutz.mdx`**

Gleiche Regeln, kompletter Body (alle 15 nummerierten Abschnitte inkl. Listen). Firmendaten im Abschnitt „Verantwortlicher" und die E-Mail-Adresse bei den Betroffenenrechten als Klartext:
```mdx
---
title: Datenschutzerklärung
intro: Informationen zum Schutz Ihrer personenbezogenen Daten gemäß DSGVO.
seoDescription: Datenschutzerklärung von Taxi Graz GU
showCompanyBlock: false
---

## 1. Datenschutz auf einen Blick

### Allgemeine Hinweise

…
```
Prüfen nach der Konvertierung: `grep -c "^## " content/legal/datenschutz.mdx` entspricht der Anzahl `<h2` in der alten Datei (`grep -c "<h2" src/app/datenschutz/page.tsx`).

- [ ] **Step 4: `src/components/legal/markdownComponents.tsx` – Typografie der Rechtsseiten**

```tsx
import type { ReactNode } from 'react'

type WithChildren = { children?: ReactNode }

/** Bildet die bisherigen Tailwind-Klassen der Rechtsseiten auf Rich-Text-Knoten ab. */
export const legalComponents = {
  h2: (props: WithChildren) => (
    <h2 className="mt-8 text-xl font-bold text-[var(--color-black)] first:mt-0">{props.children}</h2>
  ),
  h3: (props: WithChildren) => <h3 className="mt-4 font-semibold text-[var(--color-black)]">{props.children}</h3>,
  p: (props: WithChildren) => <p className="mt-3">{props.children}</p>,
  ul: (props: WithChildren) => <ul className="mt-3 list-disc pl-6 space-y-1">{props.children}</ul>,
  ol: (props: WithChildren) => <ol className="mt-3 list-decimal pl-6 space-y-1">{props.children}</ol>,
  li: (props: WithChildren) => <li>{props.children}</li>,
  lic: (props: WithChildren) => <>{props.children}</>,
  bold: (props: WithChildren) => <strong className="font-semibold text-[var(--color-black)]">{props.children}</strong>,
  italic: (props: WithChildren) => <em>{props.children}</em>,
  break: () => <br />,
  a: (props: { url?: string } & WithChildren) => {
    const external = props.url?.startsWith('http')
    return (
      <a
        href={props.url}
        className="text-[var(--color-gold-dark)] hover:underline"
        target={external ? '_blank' : undefined}
        rel={external ? 'noopener noreferrer' : undefined}
      >
        {props.children}
      </a>
    )
  },
}
```
Falls TypeScript den Schlüssel `lic` oder `break` in `components` ablehnt: beim Übergeben `components={legalComponents as never}` – die Schlüssel sind zur Laufzeit gültig (Tina-Rich-Text-Knoten `lic` = Listeneintrag-Inhalt, `break` = harter Umbruch).

- [ ] **Step 5: `src/components/legal/CompanyBlock.tsx`**

```tsx
'use client'

import { tinaField } from 'tinacms/dist/react'
import { useSettings } from '@/components/SettingsProvider'
import { phoneRaw } from '@/lib/site'

const link = 'text-[var(--color-gold-dark)] hover:underline'
const h2 = 'text-xl font-bold text-[var(--color-black)]'

/** Pflichtangaben aus den Einstellungen – bisher „Informationspflicht", „Kontakt", „UID", „Firmenbuch" im Impressum. */
export function CompanyBlock() {
  const s = useSettings()
  const host = s.seo.url.replace(/^https?:\/\//, '')

  return (
    <div className="space-y-8">
      <div data-tina-field={tinaField(s, 'company')}>
        <h2 className={h2}>Informationspflicht laut § 5 ECG</h2>
        <div className="mt-3 space-y-1">
          <p className="font-semibold text-[var(--color-black)]">{s.company.legal}</p>
          <p>Inhaber: {s.company.owner}</p>
          <p>{s.address.street}</p>
          <p>
            {s.address.zip} {s.address.city}, {s.address.country}
          </p>
        </div>
      </div>

      <div data-tina-field={tinaField(s, 'contact')}>
        <h2 className={h2}>Kontakt</h2>
        <div className="mt-3 space-y-1">
          <p>
            Telefon:{' '}
            <a href={`tel:${phoneRaw(s.contact.phone)}`} className={link}>{s.contact.phone}</a>
          </p>
          <p>
            E-Mail:{' '}
            <a href={`mailto:${s.contact.email}`} className={link}>{s.contact.email}</a>
          </p>
          <p>
            Web:{' '}
            <a href={s.seo.url} className={link}>{host}</a>
          </p>
        </div>
      </div>

      {s.company.uid && (
        <div>
          <h2 className={h2}>UID-Nummer</h2>
          <p className="mt-3">{s.company.uid}</p>
        </div>
      )}

      <div>
        <h2 className={h2}>Firmenbuchnummer &amp; Firmenbuchgericht</h2>
        <div className="mt-3 space-y-1">
          <p>Firmenbuchnummer: {s.company.register}</p>
          <p>Firmenbuchgericht: {s.company.court}</p>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 6: `src/components/legal/LegalClient.tsx`**

Kopfbereich = bisheriges Hero-Markup der Rechtsseiten (Ellipsen, Gradient, `pt-44 pb-20`), unverändert:
```tsx
'use client'

import { useTina, tinaField } from 'tinacms/dist/react'
import { TinaMarkdown, type TinaMarkdownContent } from 'tinacms/dist/rich-text'
import type { LegalQuery } from '@tina/__generated__/types'
import { Section } from '@/components/layout/Section'
import { CompanyBlock } from './CompanyBlock'
import { legalComponents } from './markdownComponents'

export interface LegalClientProps {
  data: LegalQuery
  query: string
  variables: { relativePath: string }
}

export function LegalClient(props: LegalClientProps) {
  const { data } = useTina(props)
  const page = data.legal

  return (
    <>
      <section className="relative overflow-hidden bg-[var(--color-gray-900)] pt-44 pb-20">
        <div className="absolute -left-[20%] top-[20%] h-[50vh] w-[50vh] rounded-full bg-gradient-to-br from-[var(--color-gold)]/12 via-[var(--color-gold)]/3 to-transparent blur-3xl" />
        <div className="absolute -right-[15%] bottom-[10%] h-[40vh] w-[40vh] rounded-full bg-[var(--color-gold)]/[0.05] blur-2xl" />
        <div className="absolute inset-x-0 top-0 h-40 bg-gradient-to-b from-[var(--color-gray-900)] via-[var(--color-gray-900)]/60 to-transparent" />
        <div className="relative z-10 mx-auto max-w-7xl px-5 sm:px-6 lg:px-8">
          <span className="text-xs font-bold uppercase tracking-[0.25em] text-[var(--color-gold)]">Rechtliches</span>
          <h1
            data-tina-field={tinaField(page, 'title')}
            className="mt-4 font-[var(--font-display)] text-4xl font-bold tracking-tight text-white sm:text-5xl"
          >
            {page.title}
          </h1>
          <p data-tina-field={tinaField(page, 'intro')} className="mt-3 text-[var(--color-gray-400)]">
            {page.intro}
          </p>
        </div>
      </section>

      <Section className="bg-[var(--color-cream)]">
        <div className="space-y-8 text-[var(--color-gray-600)] leading-relaxed">
          {page.showCompanyBlock && <CompanyBlock />}
          <div data-tina-field={tinaField(page, 'body')}>
            <TinaMarkdown content={page.body as TinaMarkdownContent} components={legalComponents} />
          </div>
        </div>
      </Section>
    </>
  )
}
```

- [ ] **Step 7: Seiten umstellen**

`src/app/impressum/page.tsx`:
```tsx
import type { Metadata } from 'next'
import client from '@tina/__generated__/client'
import { createMetadata } from '@/lib/metadata'
import { LegalClient } from '@/components/legal/LegalClient'

const RELATIVE_PATH = 'impressum.mdx'

export async function generateMetadata(): Promise<Metadata> {
  const [settings, page] = await Promise.all([
    client.queries.settings({ relativePath: 'site.json' }),
    client.queries.legal({ relativePath: RELATIVE_PATH }),
  ])
  return createMetadata(settings.data.settings, {
    title: page.data.legal.title,
    description: page.data.legal.seoDescription,
    path: '/impressum',
    noIndex: true,
  })
}

export default async function ImpressumPage() {
  const res = await client.queries.legal({ relativePath: RELATIVE_PATH })
  return <LegalClient data={res.data} query={res.query} variables={res.variables} />
}
```
`src/app/datenschutz/page.tsx`: identisch mit `RELATIVE_PATH = 'datenschutz.mdx'`, `path: '/datenschutz'`, Funktionsname `DatenschutzPage`.

- [ ] **Step 8: `constants.ts` entfernen**

```bash
git rm src/lib/constants.ts
grep -rn "SITE_CONFIG\|@/lib/constants" src tina || echo "keine Referenzen mehr"
```

- [ ] **Step 9: Verifizieren**

Run: `npx tsc --noEmit && npm run lint 2>&1 | tail -2`
Expected: sauber; Lint jetzt 1 Error (CookieConsent) und 0 Warnings.

Run: Dev-Server-Befehl, dann
```bash
curl -s -X POST http://localhost:4001/graphql -H 'content-type: application/json' \
  -d '{"query":"{ legal(relativePath:\"impressum.mdx\"){ title showCompanyBlock } }"}'; echo
curl -s http://localhost:3000/impressum | grep -o "Informationspflicht laut § 5 ECG\|Firmenbuchnummer: FN 523619 d\|Haftungsausschluss\|Webdesign &amp; Umsetzung" | sort -u
curl -s http://localhost:3000/datenschutz | grep -c "<h2"
curl -s http://localhost:3000/datenschutz | grep -o '<meta name="robots" content="noindex[^"]*"'
zsh "$SCRATCH/shots.sh" "$SCRATCH/task9"
```
Expected: `showCompanyBlock: true`; alle vier Impressum-Strings; H2-Anzahl der Datenschutzseite = Anzahl `## ` in der MDX; noindex-Meta vorhanden. `impressum-1440.png` und `datenschutz-1440.png` vorher/nachher per Read-Tool vergleichen: Abstände zwischen Abschnitten (~2rem), Überschriftgrößen und Linkfarbe gleich. Stopp-Befehl.

- [ ] **Step 10: Commit**

```bash
git add -A tina/config.ts tina/collections/legal.ts tina/tina-lock.json content/legal src/components/legal src/app/impressum/page.tsx src/app/datenschutz/page.tsx src/lib/constants.ts
git commit -m "feat(cms): Impressum und Datenschutz als Rich-Text, constants.ts entfernt"
```

---

### Task 10: Endabnahme, Cloud-Checkliste und Kundenanleitung

**Files:**
- Create: `docs/tina-cloud-setup.md`
- Modify: `docs/superpowers/specs/2026-09-01-tina-cms-design.md` (Abschnitt „Abweichungen" ergänzen)

**Interfaces:**
- Consumes: alles aus Task 1–9; Baseline-Screenshots `$SCRATCH/before`.

- [ ] **Step 1: Production-Build lokal (ohne Tina Cloud)**

Run: `npm run build:local 2>&1 | tail -25`
Expected: `tinacms build` legt `public/admin/index.html` an, danach `next build` mit „✓ Generating static pages" für `/`, `/kontakt`, `/impressum`, `/datenschutz`, `/opengraph-image`, `/sitemap.xml`, `/robots.txt`. Keine Fehler. (Ohne `--local` würde der Build wegen fehlender Cloud-Credentials scheitern – das ist erwartet und Felix' Schritt.)

- [ ] **Step 2: Tests + Lint + Typen**

Run: `npm test && npx tsc --noEmit && npm run lint 2>&1 | tail -2`
Expected: alle Tests grün; `tsc` sauber; Lint: 1 Error (CookieConsent, Baseline), 0 Warnings.

- [ ] **Step 3: Screenshot-Vergleich vorher/nachher**

Run: Dev-Server-Befehl, dann
```bash
zsh "$SCRATCH/shots.sh" "$SCRATCH/after"
for f in home kontakt impressum datenschutz; do for w in 1440 390; do
  printf "%s-%s: " "$f" "$w"; magick compare -metric AE "$SCRATCH/before/$f-$w.png" "$SCRATCH/after/$f-$w.png" "$SCRATCH/after/$f-$w-diff.png" 2>&1; echo
done; done
```
Expected: Für jede Seite Vorher/Nachher (und bei größeren Pixelzahlen das `-diff.png`) mit dem Read-Tool ansehen. Zulässig: Marquee-Verschiebung, Animations-Timing, Cookie-Banner. **Nicht zulässig:** verschobene Abstände, andere Schriftgrößen, fehlende Elemente, andere Farben. Abweichungen fixen, bevor es weitergeht.

- [ ] **Step 4: Admin-Durchlauf (Visual Editing lokal)**

Mit den Playwright-MCP-Tools (`browser_navigate`, `browser_snapshot`, `browser_click`, `browser_type`, `browser_take_screenshot`):
1. `http://localhost:3000/admin/index.html` öffnen → Sidebar zeigt „Einstellungen" (Globus), „Startseite", „Kontaktseite", „Rechtliches".
2. „Startseite" → Dokument `home` anklicken → die Startseite erscheint als Vorschau im Iframe, rechts das Formular.
3. Im Formular `Hero → Badge-Text` auf „Test-Badge" ändern → Vorschau zeigt sofort „Test-Badge" (ohne Speichern).
4. „Einstellungen" → `Design → Akzentfarbe` auf `#2E86DE` ändern → Buttons, Logo, Marquee und Grid in der Vorschau werden blau.
5. **Nicht speichern**; Formular verwerfen. Danach `git status --short` → keine geänderten `content/`-Dateien.
6. Zusätzlich einmal Speichern testen: `Hero → Badge-Text` ändern → „Save" → `git diff --stat content/pages/home.json` zeigt 1 geänderte Zeile → `git checkout content/pages/home.json`.
Expected: alle sechs Punkte erfüllt. Falls die Vorschau im Iframe leer bleibt: `curl -sI http://localhost:3000/ | grep -i frame` muss leer sein (kein `X-Frame-Options`).

- [ ] **Step 5: Kontaktformular-Route unverändert**

Run: `curl -s -X POST http://localhost:3000/api/contact -H 'content-type: application/json' -d '{}' -w '\n%{http_code}\n'`
Expected: Validierungsfehler mit `400` (kein Mailversand). Danach Stopp-Befehl.

- [ ] **Step 6: `docs/tina-cloud-setup.md` schreiben**

```markdown
# Tina Cloud – Einrichtung für taxigraz-gu.at

Stand: 2026-09-01. Lokal läuft das CMS ohne Cloud (`npm run dev` → http://localhost:3000/admin). Für die Live-Seite braucht es einmalig Tina Cloud (Free-Plan: 2 Nutzer, 1 Projekt, 0 €).

## 1. Projekt in Tina Cloud anlegen (Felix)

1. https://app.tina.io → „Sign in with GitHub" (Account `Felixzink96`).
2. „New Project" → „Import your site" → Repository `Felixzink96/taxi-graz-gu`, Branch `main`.
3. **Site URL(s):** `https://taxigraz-gu.at` und `http://localhost:3000` (für lokales Visual Editing gegen die Cloud).
4. Nach dem Anlegen: **Overview → Client ID** kopieren. **Tokens → „Content (Read-only)"** erstellen und kopieren.

## 2. Vercel-Umgebungsvariablen

Vercel → Projekt `taxi-graz-gu` → Settings → Environment Variables (Production **und** Preview):

| Name | Wert |
|---|---|
| `NEXT_PUBLIC_TINA_CLIENT_ID` | Client ID aus Schritt 1 |
| `TINA_TOKEN` | Read-only-Token aus Schritt 1 |

Build Command bleibt `npm run build` (= `tinacms build && next build`).

## 3. Branch mergen und deployen

```bash
git checkout main && git merge --no-ff feat/tina-cms
git push vercel main && git push origin main
```
Vercel baut; im Build-Log muss „tinacms build" ohne Fehler durchlaufen. Danach https://taxigraz-gu.at/admin öffnen → Login mit dem Tina-Account.

## 4. Kunden einladen

Tina Cloud → Projekt → **Collaborators → Invite** → E-Mail des Kunden, Rolle „Editor". Der Kunde bekommt eine Einladung und legt sein Passwort fest.

## 5. Wichtig für den laufenden Betrieb

- Jede Änderung im Admin = ein Commit auf `main` im Repo `Felixzink96/taxi-graz-gu` → Vercel deployt in 1–2 Minuten.
- Das Spiegel-Repo `unicornhub1/Taxi-Graz-GU` (`origin`) läuft dadurch hinterher. Vor eigenen Änderungen: `git pull vercel main && git push origin main`.
- Bilder: unter 1 MB halten (Media-Limit 100 MB im Free-Plan). Ordner im Repo: `public/uploads/`.
- Neue Felder/Sektionen = Schema-Änderung in `tina/collections/*.ts` → committen, Vercel-Build aktualisiert das Schema automatisch in Tina Cloud.

## 6. Kurzanleitung für den Kunden

1. https://taxigraz-gu.at/admin öffnen und anmelden.
2. Links „Startseite", „Kontaktseite" oder „Rechtliches" wählen – die Seite erscheint als Vorschau, rechts die Felder. Text in der Vorschau anklicken springt zum passenden Feld.
3. „Einstellungen" (Globus-Symbol): Telefonnummer, E-Mail, Adresse, Google-Bewertung, **Akzentfarbe**, Logo und Hintergrundbild.
4. Platzhalter `{phone}`, `{email}`, `{rating}`, `{reviews}` in Texten werden automatisch durch die Einstellungen ersetzt.
5. „Save" klicken – nach 1–2 Minuten ist die Änderung online. Nichts kaputtmachen können: Layout und Design sind fest, nur Inhalte ändern sich.
```

- [ ] **Step 7: Spec um die Abweichungen ergänzen**

Am Ende von `docs/superpowers/specs/2026-09-01-tina-cms-design.md` einen Abschnitt `## Abweichungen in der Umsetzung (2026-09-01)` anfügen – die fünf Punkte aus dem Plan-Header (Footer-Text in Einstellungen, keine Custom-Queries, TrustBar ohne Icons, `showCompanyBlock`-Flag, Impressum-Reihenfolge).

- [ ] **Step 8: Commit + Abschlussbericht**

```bash
git add docs/tina-cloud-setup.md docs/superpowers/specs/2026-09-01-tina-cms-design.md docs/superpowers/plans/2026-09-01-tina-cms.md
git commit -m "docs: Tina-Cloud-Checkliste, Kundenanleitung und Spec-Abweichungen"
git log --oneline main..feat/tina-cms
```
Bericht an Felix: Commits auf `feat/tina-cms`, Ergebnis des Screenshot-Vergleichs, Lint-/Test-/Build-Status, und die Checkliste aus `docs/tina-cloud-setup.md` – **kein Merge, kein Push** ohne sein OK.

---

### Task 11: Navigation, Kontakt-Beschriftungen und Footer-Texte ins CMS (Nachforderung Felix, 2026-09-01)

**Hintergrund:** Felix: „Header, Top-Bar sowie Footer und CTA müssen vollständig bearbeitbar sein, sowie alles, wo E-Mail, Telefonnummer, WhatsApp-Nummer, Google-Bewertungszahl etc. steht." Zahlen/Nummern sind bereits global; es fehlen Navigation, die Beschriftungen neben den Kontaktdaten und die Footer-Überschriften. Läuft nach Task 9, vor Task 10.

**Files:**
- Modify: `tina/collections/settings.ts`, `content/settings/site.json`, `src/components/layout/Header.tsx`, `src/components/layout/Footer.tsx`, `src/components/sections/ContactBar.tsx`, `src/components/sections/FloatingContact.tsx`, `src/components/sections/Hero.tsx`, `src/components/sections/CTA.tsx`, `src/app/kontakt/KontaktClient.tsx`
- Delete: `src/data/navigation.ts`, `src/types/index.ts` (nur noch `NavItem`, sonst ungenutzt)

**Interfaces:**
- Consumes: `useSettings()`, `compact()`, `tinaField`.
- Produces: `settings.navigation.main[] / legal[]` `{ label, href }`; `settings.labels.{phone,phoneSub,whatsapp,whatsappSub,email,address,call,callNow,reviews,googleReviews}`; `settings.footer.{description,navHeading,legalHeading,hotlineHeading,copyright}`.
- Bewusst im Code: Footer-Credit „Design by Unicorn Factory" (Agentur-Attribution), aria-Labels („Menü öffnen/schließen"), Verbindungswörter („oder", „|").

**Design-Regel:** wie in Task 5/6 – nur Datenquellen, `data-tina-field` und Textknoten ändern; Klassen/Markup identisch.

- [ ] **Step 1: Schema erweitern – `tina/collections/settings.ts`**

Im `footer`-Objekt die Felder ergänzen (nach `description`):
```ts
{ type: 'string', name: 'navHeading', label: 'Überschrift Navigation', required: true },
{ type: 'string', name: 'legalHeading', label: 'Überschrift Rechtliches', required: true },
{ type: 'string', name: 'hotlineHeading', label: 'Überschrift Hotline', required: true },
{ type: 'string', name: 'copyright', label: 'Copyright-Zusatz (nach Jahr und Firma)', required: true },
```
Zwei neue Objektgruppen **vor** `design` einfügen:
```ts
{
  type: 'object', name: 'navigation', label: 'Navigation', required: true,
  fields: [
    {
      type: 'object', name: 'main', label: 'Hauptmenü', list: true, required: true,
      ui: { itemProps: (item) => ({ label: item?.label }) },
      fields: [
        { type: 'string', name: 'label', label: 'Beschriftung', required: true },
        { type: 'string', name: 'href', label: 'Link', required: true, description: 'z. B. /#leistungen oder /kontakt' },
      ],
    },
    {
      type: 'object', name: 'legal', label: 'Rechtliches (Footer)', list: true, required: true,
      ui: { itemProps: (item) => ({ label: item?.label }) },
      fields: [
        { type: 'string', name: 'label', label: 'Beschriftung', required: true },
        { type: 'string', name: 'href', label: 'Link', required: true },
      ],
    },
  ],
},
{
  type: 'object', name: 'labels', label: 'Beschriftungen (Kontakt-Elemente)', required: true,
  description: 'Wird überall verwendet, wo Telefon, WhatsApp und E-Mail angezeigt werden.',
  fields: [
    { type: 'string', name: 'phone', label: '„Telefon"', required: true },
    { type: 'string', name: 'phoneSub', label: 'Zusatz unter Telefon', required: true },
    { type: 'string', name: 'whatsapp', label: '„WhatsApp"', required: true },
    { type: 'string', name: 'whatsappSub', label: 'Zusatz unter WhatsApp', required: true },
    { type: 'string', name: 'email', label: '„E-Mail"', required: true },
    { type: 'string', name: 'address', label: '„Adresse"', required: true },
    { type: 'string', name: 'call', label: 'Kurzer Anruf-Button (mobil)', required: true },
    { type: 'string', name: 'callNow', label: '„Jetzt anrufen"', required: true },
    { type: 'string', name: 'reviews', label: '„Bewertungen"', required: true },
    { type: 'string', name: 'googleReviews', label: '„Google-Bewertungen"', required: true },
  ],
},
```

- [ ] **Step 2: Inhalte – `content/settings/site.json`**

`footer` ersetzen und `navigation`/`labels` vor `design` ergänzen:
```json
"footer": {
  "description": "Ihr zuverlässiges Taxiunternehmen in Graz und Graz-Umgebung. 24/7 erreichbar für alle Ihre Fahrten.",
  "navHeading": "Navigation",
  "legalHeading": "Rechtliches",
  "hotlineHeading": "24h Taxi-Hotline",
  "copyright": "Alle Rechte vorbehalten."
},
"navigation": {
  "main": [
    { "label": "Leistungen", "href": "/#leistungen" },
    { "label": "Service-Gebiete", "href": "/#gebiete" },
    { "label": "Bewertungen", "href": "/#bewertungen" },
    { "label": "FAQ", "href": "/#faq" },
    { "label": "Kontakt", "href": "/kontakt" }
  ],
  "legal": [
    { "label": "Impressum", "href": "/impressum" },
    { "label": "Datenschutz", "href": "/datenschutz" }
  ]
},
"labels": {
  "phone": "Telefon",
  "phoneSub": "24/7 erreichbar",
  "whatsapp": "WhatsApp",
  "whatsappSub": "Nachricht senden",
  "email": "E-Mail",
  "address": "Adresse",
  "call": "Anrufen",
  "callNow": "Jetzt anrufen",
  "reviews": "Bewertungen",
  "googleReviews": "Google-Bewertungen"
},
```

- [ ] **Step 3: Komponenten umstellen (Ersetzungstabellen)**

Gemeinsam: `const { labels, navigation, footer } = settings` nach Bedarf destrukturieren; `const mainNav = compact(navigation.main)`, `const legalNav = compact(navigation.legal)`.

`Header.tsx` (Import `navigation` aus `@/data/navigation` entfernen):
| bisher | neu |
|---|---|
| `navigation.main.map((item) =>` (Desktop) | `mainNav.map((item) =>` + `data-tina-field={tinaField(item)}` auf dem `<Link>` |
| `navigation.main.map((item, i) =>` (Mobil) | `mainNav.map((item, i) =>` + `data-tina-field={tinaField(item)}` auf dem `<button>` |
| `<span className="sm:hidden">Anrufen</span>` | `<span className="sm:hidden" data-tina-field={tinaField(labels, 'call')}>{labels.call}</span>` |
| `Jetzt anrufen` (Mobilmenü) | `<p … data-tina-field={tinaField(labels, 'callNow')}>{labels.callNow}</p>` |
| `WhatsApp` / `Nachricht senden` (Mobilmenü) | `{labels.whatsapp}` / `{labels.whatsappSub}` mit `data-tina-field` |
| `E-Mail` (Mobilmenü) | `{labels.email}` mit `data-tina-field` |
| `({settings.google.reviews} Bewertungen)` | `({settings.google.reviews} {labels.reviews})` |

`Footer.tsx` (Import `navigation` entfernen):
| bisher | neu |
|---|---|
| `Navigation` / `Rechtliches` / `24h Taxi-Hotline` (h3) | `{footer.navHeading}` / `{footer.legalHeading}` / `{footer.hotlineHeading}` mit `data-tina-field={tinaField(footer, '…')}` |
| `navigation.main.map` / `navigation.legal.map` | `mainNav.map` / `legalNav.map` + `data-tina-field={tinaField(item)}` auf dem `<Link>` |
| `Alle Rechte vorbehalten.` | `<span data-tina-field={tinaField(footer, 'copyright')}>{footer.copyright}</span>` |
| `Design by … Unicorn Factory` | unverändert |

`ContactBar.tsx`: `WhatsApp` → `<span className="font-medium" data-tina-field={tinaField(labels, 'whatsapp')}>{labels.whatsapp}</span>`.

`FloatingContact.tsx`: `Anrufen` → `{labels.call}`, `WhatsApp` → `{labels.whatsapp}` (jeweils mit `data-tina-field` auf dem umgebenden `<motion.a>` … nein: auf einem `<span>` um den Text, damit Klassen unverändert bleiben).

`Hero.tsx` (Kontaktkarte + Eyebrow):
| bisher | neu |
|---|---|
| `({settings.google.reviews} Bewertungen)` (Eyebrow) | `({settings.google.reviews} {labels.reviews})` |
| `Telefon` / `24/7 erreichbar` | `{labels.phone}` / `{labels.phoneSub}` |
| `WhatsApp` / `Nachricht senden` | `{labels.whatsapp}` / `{labels.whatsappSub}` |
| `E-Mail` | `{labels.email}` |
| `({settings.google.reviews} Google-Bewertungen)` | `({settings.google.reviews} {labels.googleReviews})` |
jeweils `data-tina-field={tinaField(labels, '<feld>')}` auf dem `<p>`.

`CTA.tsx` (drei Karten):
| bisher | neu |
|---|---|
| `Anrufen` | `{labels.call}` |
| `WhatsApp` / `Nachricht senden` | `{labels.whatsapp}` / `{labels.whatsappSub}` |
| `E-Mail` | `{labels.email}` |
jeweils `data-tina-field` auf dem `<span>`.

`KontaktClient.tsx` (Karten-Titel; `page.cards.phoneSub/whatsappSub` bleiben seitenspezifisch):
| bisher | neu |
|---|---|
| `<h3 …>Telefon</h3>` | `{labels.phone}` |
| `<h3 …>WhatsApp</h3>` / `Nachricht senden` (p) | `{labels.whatsapp}` / `{labels.whatsappSub}` |
| `<h3 …>E-Mail</h3>` | `{labels.email}` |
| `<h3 …>Adresse</h3>` | `{labels.address}` |
jeweils `data-tina-field={tinaField(labels, '<feld>')}`.

- [ ] **Step 4: Alte Dateien entfernen**

```bash
git rm src/data/navigation.ts src/types/index.ts
grep -rn "@/data/navigation\|@/types" src || echo "keine Referenzen mehr"
```

- [ ] **Step 5: Verifizieren**

Dev-Server (Ports vorher frei machen), dann: `npx tsc --noEmit` sauber; `npm run lint` = 1 Error (CookieConsent) / 0 Warnings; GraphQL `{ settings(relativePath:"site.json"){ navigation { main { label href } } labels { call } footer { copyright } } }` liefert die Werte; `curl -s http://localhost:3000/ | grep -o 'Service-Gebiete\|24h Taxi-Hotline\|Alle Rechte vorbehalten\|Nachricht senden' | sort -u` → alle vier; Screenshots `node "$SCRATCH/shots-pw.mjs" "$SCRATCH/task11"` und `magick compare` gegen `$SCRATCH/before` für home/kontakt (1440 + 390) – keine Layoutabweichung.

- [ ] **Step 6: Commit**

```bash
git add tina/collections/settings.ts tina/tina-lock.json content/settings/site.json src/components src/app/kontakt/KontaktClient.tsx src/data src/types
git commit -m "feat(cms): Navigation, Kontakt-Beschriftungen und Footer-Texte editierbar"
```
